# Project-local intent. The active Codex wrapper also permits this documented
# 32 KiB limit so HSWM's AGENTS.md is included in new sessions.
project_doc_max_bytes = 32768
project_doc_fallback_filenames = []
