# HSWM Solid Multi-Agent — Absorb Contract v1

> **Status**: LOCKED for stack absorption (2026-07-22)
>
> **Tree**: `LakatosTree_HSWM_SolidMultiAgent_20260722`
>
> **Evidence anchors**: SAGPM, M2, S3, multi-seed MuSiQue, **2Wiki external**

## Deploy slogan (ship)

```
Admit flat. Expand gated (optional). Govern late.
TRAVERSAL_OFF. Fuse weight may be 0.
```

## Default multi-agent handoff path

| Stage | Layer | Rule |
|---|---|---|
| A1 ADMIT | L1 Semantic Field | **flat residual / cosine primary only** |
| A2 EXPAND | L2 Hyper | optional, from A1 seeds only, fixed budget |
| A3 GOVERN | L4 Governance | **late supersede filter** (hard drop fully superseded if live remains) |
| HANDOFF OBJ | L3 Facade | `{doc_id, rank, score, version, supersede_flag, path}` |

Equal-compute: fuse/gate **must not** open extra top-k/encode slots vs flat control.

## Ship now / later / never

| Ship now | Ship later | Never claim-ship |
|---|---|---|
| flat primary admit | fuse as M default | structure-primary ranking |
| L4 late supersede gate | CHU multi-writer WAL | query-time traversal ON as multi-hop fix |
| L1 static residual field opt-in | learning-while-using stream | cognitive uplift vs direct LLM |
| TRAVERSAL_OFF default | 나생문 auto into PROM | deep propagation stack for recall |
| LakatoTree prereg→measure→submit | true 2Wiki multi-seed beyond n=200 | default-on semantic weave (PROM P4 partial) |
| S3 dual-channel write identity | | progressive from markdown alone |

## Layer ports (agent operators)

| Agent | Read | Write |
|---|---|---|
| Retriever | L1, L2 | — |
| Research / PROM | L0–L2 | L3 findings only |
| Critic / 나생문 | L0, L3 | L4 judgment/supersede events (user-explicit) |
| Planner | L3 | L3 plans |
| Occam | L0–L4 | L4 supersede dose |
| Longinus | field_id | locator update on Field (not N rebinds) |

## Measured locks (do not reopen without new prereg)

| Claim | Result |
|---|---|
| structure-primary handoff | **REFUTED** (MuSiQue −0.28; 2Wiki joint 0.59) |
| flat+L4 joint vs flat | **PASS external** (2Wiki +0.10; MuSiQue multi-seed min +0.03) |
| G stale cut | **PASS** multi-seed + 2Wiki |
| S absolute under writes | **PASS S3** collapse 0.446 < 0.5 (fixture) |
| fuse required for M | **NO** (ARM3−ARM2 = 0) |

## Anti-absorb RED (skill must fail closed)

1. Deploy/docs claim structure-primary ranking for handoff
2. traversal_mu > 0 as default multi-hop solution
3. blind RRF as only fusion strategy
4. progressive M claim without equal-compute receipt
5. cognitive uplift product copy
6. deep GNN stack as performance default
7. Mac-internal storage required for bench data when GM path exists

## Executable boundary

The helper CLI used during the originating SYMPOSIUM cycle is not vendored in this
repository. It is therefore **not** part of the public reproducibility claim. The
repository-enforced boundary is the inactive admission manifest and its fail-closed
validator under `_research/competitor_absorption/`.

## 비행기맨 VR (ValidationResult) honesty fields

VR = KG `:ValidationResult` 노드. “출격이 진짜였는지” 도장.

| Field | Role |
|---|---|
| `dispatch_mode` | `AGENT_TOOL_N` / `ENGINE_DISPATCH` / `FS_AUTOMATION+hostile` … |
| `subagent_count` | 실제 독립 agent 수 |
| `n_eff` | 상관 보정 유효 독립 수 (나생문) |
| `automation_flag` | 자동화 여부 |
| `hswm_mode` | `off` \| `flat_L4` \| `optin_weave` |
| `traversal_mu` | must be `0` for default ship |
| `readout` | `flat_L4` (deploy) |

The originating PROM skill pointer remains external to this repository and is not
required to validate the published contract.
