# HSWM existence scoreboard v1 (2026-08-03)

> **program**: **HSWM core 개발** 안의 실존 장부 (이름 정본: `HSWM_CORE_DEV.md`).
> **purpose**: “HSWM이 실존하냐?”에 **문서 말고 과학 층**으로 답하는 한 장.
> **authority**: SECONDARY crystallization of already-registered sealed scopes. Not a new measurement.
> **not**: full E2E neural glue replacement proof. That is **X1 / F1** — still open.
> **고도화**: seal 결과에 따라 이 표를 개정한다. 계획이 완벽하지 않음.

## 0. 한 줄

| 질문 | 지금 정직한 답 |
|---|---|
| HSWM이 **뭔가 실재하는 측정 대상**이냐? | **부분적으로 예** — substrate·credit·topology 축에 sealed 결과가 있다 |
| HSWM이 **지향 정체(멀티에이전트 glue의 신경망 대체)** 로 증명됐냐? | **아직 아니오** — X1(F1 typed function network) **NOT_MEASURED** |
| 분산 p2p public 서비스가 실존 증거냐? | **아니오** — 코어 아님, 나중 층 |

## 1. 실존 사다리 (X)

| id | claim (짧게) | status | 코어 의미 |
|---|---|---|---|
| **X0** | 메모리/검색 substrate로 일반 dense 대비 우위 (등록 스코프) | **SUPPORTED (scoped)** | 부분 실존 — “장”이 검색 기질로는 실재 |
| **X1** | typed multi-LLM function network 고유 인과 우위 | **NOT_MEASURED (sealed)** — dev 탐색: coverage v1 무승부(+0.000) → utility v2로 typed 전 대조군 승리(+0.296, 탐색적), sealed-v2 재계약 중 (`F1_R8_RESULTS_LOG.md`) | **주 실존 전선** (E2E glue 대체) |
| **X2** | credit → 유용한 ΔW | **SUPPORTED (scoped, F2 seal)** | 학습 신호 부분 실존 |
| **X3** | 학습된 sparse topology가 일함 | **SUPPORTED (scoped, F4 seal)** | 구조 학습 부분 실존 |
| **X4** | A→B transfer | **KILLED on registered bed (F3)** | 그 테스트베드에선 전이 실존 실패 |
| **X5** | sleep consolidation | **KILLED (F5)** | 그 연산자 실존 실패 |
| **X6** | 합의=場 merge 동역학 | OPEN | 지금 메인 금지 |
| **X7** | 거대 public p2p 서비스 | NOT_CORE | 집중 기간 하드 밴 |

상세 경로·수치는 `INDEX.md` / `GIT/HSWM/receipts/` / F2–F5 sealed JSON.
이 표는 **상태 요약**이지 수치 재주장 원본이 아니다.

## 2. “빠른 과학 성과” 정의 (집중 창)

| 등급 | 무엇 | 언제 |
|---|---|---|
| **Fast win (오늘~1일)** | 이 스코어보드 + concentration harness가 “부분 실존 / X1 미결”을 고정 | T0 |
| **Real core win (수일)** | F1 sealed metric 1줄 또는 등록된 clean INCONCLUSIVE | T1 |
| **가짜 진도 (세지 않음)** | Longinus rebind만, 333 green, 연결성 문서만, MCP 이주 | — |

## 3. 다음 손 (강제)

1. **T0**: 이 파일 유지 (done when harness T0 checks PASS).
2. **T1**: `KEEP_A2` 기본 → F1 zero-call → development power → prereg → sealed.
   - 실행 정본: `FINDINGS/hswm-f1-r8-try3-2026-07-28/F1_R8_RUNBOOK.md`
   - 지휘: `f1_r8_operator_harness.py` + `hswm_core_existence_harness.py`
3. **X7/333/public**: X1이 움직이기 전 **메인 성과로 치지 않음**.

## 4. non-claim

- 부분 실존 ≠ 테슬라식 E2E 완주.
- kill 결과도 과학 성과다 (부정 실존).
- 라카토트리는 장부이지 HSWM 본체가 아니다.
