# H3-B3 V3 refusal receipt — 2026-07-20

## Verdict

`REFUSED_BEFORE_EFFICACY`.

The V3 development extractor did not produce a complete compiler-admissible
domain.  No extraction CLOSE, embedding OPEN, development score, transition,
or fresh artifact was created.  This receipt is an infrastructure refusal, not
an HSWM efficacy result.

## Frozen chain

| item | value |
|---|---|
| original protocol SHA-256 | `338a8859a7e2eebbea9c804d75f6b8e0db09d7ddf6b91db939cb30bae9f59a31` |
| V3 manifest SHA-256 | `7f9ec247afbdd11066706837a921159da6480d1c013995dea23b7c3907c284bb` |
| V3 preflight file SHA-256 | `bbe6b3525fb01e4a58a9076ae0ae846a21b3018f95a3e326e19a3d33e3dec1e5` |
| extraction OPEN file SHA-256 | `a5c008974bcdc56ced1702d9d23f909d96f6d49aa7731917afb9ac718bad6b63` |
| extraction JSONL SHA-256 after the sole retry | `bab9d5bb4d152c3f65e15f5fb2f876c37846fbb09aa101442c7b87f7ca54ef1b` |
| extraction JSONL inode / bytes | `410657328` / `31,493,335` |
| extractor config | batch `1`, concurrency `2`, timeout `180s`, `max_tokens=512` |

## Execution evidence

The initial pass made 3,599 endpoint calls.  Its terminal view was:

- success `1,661`
- partial `1,421`
- quarantined `514`
- error `3`

The prereg-compatible command was then rerun exactly once.  It produced 3,596
cache hits and three endpoint calls.  One source recovered to `partial/stop`;
two again ended at exactly 512 completion tokens with
`finish_reason=length`.  The final view was success `1,661`, partial `1,422`,
quarantined `514`, error `2`.  The append-only file contains 3,602 physical
attempt rows and only 3,597 compiler-admissible sources.

| unresolved source | attempt 1 content SHA-256 | attempt 2 content SHA-256 |
|---|---|---|
| `The Unwinding` (`...ae3b4bcf`) | `34776c749143882e641586317148f1b8d3b2a2b26960cf52e4bcdbf3cc07d7f4` | same |
| `Krešimir Čuljak` (`...d43bd402`) | `2a448fe0a707187b55e6e38d360ee0ab078602678c6dbe79a456a219394a4c1c` | same |

Across the 3,597 non-length attempts, nearest-rank completion-token p99 was
403 and the maximum was 507; the maximum prompt was 1,277 tokens.  The two
repeated byte-identical content prefixes therefore identify a deterministic
output-cap failure, not a transient transport failure.

## Safety actions

- V3 `extractions.close.json` remains absent.
- The development embedding, development report, certificate transition, and
  fresh extraction OPEN all remain absent.
- The old OMD owner was fenced after heartbeat expiry.  A replacement lease
  appended only the three authorized retry rows; its task was then voluntarily
  bailed and cancelled with this refusal reason.
- V3 is immutable historical evidence.  It must not be appended, closed,
  cherry-picked, or copied into a successor cache.

## Successor rule

Any successor must use a new extractor schema, code root, preflight receipt,
manifest, OPEN receipt, inode, and output prefix.  The entire 3,599-source
development extraction must be rerun before any efficacy metric is opened.
The governing successor protocol is
`H3_B3_V4_RESTART_PREREG_2026-07-20.md`.
