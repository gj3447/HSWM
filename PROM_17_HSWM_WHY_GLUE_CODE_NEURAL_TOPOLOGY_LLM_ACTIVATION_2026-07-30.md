# PROM-17 — 왜 HSWM이어야 하는가: Glue Code 제거와 LLM 활성화 함수의 두 축

> **status**: `POSITION_PAPER / MOTIVATION_CANON_CANDIDATE`
> **date**: 2026-07-30
> **authority**: USER_CANON 대화(2026-07-30)에서 출발한 SECONDARY_AI synthesis.
> 이 문서는 **동기·정체성 선언**이며 측정된 효능 주장이 아니다. 실험적 승격은
> [`research/HSWM_RESEARCH_LEDGER.v1.json`](research/HSWM_RESEARCH_LEDGER.v1.json)의
> fail-closed 7단계 게이트를 별도로 통과해야 한다.
> **relation**: README의 `Core Motivation: Replacing Hardcoded Agent Glue with
> Learned Neural Topology` 절(2026-07-30)을 확장·정식화한다. 목표 아키텍처의
> 수식 정합은 PROM-9, 총체 과학 구조는 PROM-16, 정체성 정전은
> `CANON_DIRECTION_NEURAL_COGNITIVE_ENTITY_2026-07-23.md`과 연결된다.
> **Naesengmoon**: 본 문서는 위치 논문으로 적대적 검증 대상 실험이 아니다.

## 0. 결론

HSWM의 존재 이유는 두 개의 축으로 정식화된다.

1. **축 1 (부정)**: 현재 AI 에이전트·MCP·CLI 생태계는 자율주행 초기의 rule-based
   C++ 스택과 같은 물질 — 하드코딩된 `if-else` 라우터, 프롬프트 체인, 손으로 쓴
   오케스트레이션 스크립트 — 로 만들어져 있고, 에이전트×도구×맥락의 조합 폭발
   앞에서 구조적으로 붕괴한다. 자율주행이 End-to-End 신경망으로 이 벽을 돌파했듯,
   협력·라우팅·기억 수송도 학습되는 가중치 장으로 대체해야 한다.
2. **축 2 (긍정)**: 그 대처이자 주체는 "LLM에 도구를 붙인 것"이 아니라,
   **거시 신경망 그 자체인 HSWM**이다. LLM은 이 거시 신경망의 노드를 점화시키는
   비선형 활성화 함수(semantic activation function)로 격하된다. 주객(主客)의
   전도다.

\[
f_i^t := \operatorname{LLM}(\rho_i, x_i^t, a_{\mathcal N(i)}^t),
\qquad
\mathrm{HSWM}_t := (H_t, W_t, A_t, \{f_i^t\})
\]

두 축은 하나의 논증의 앞뒤다: 축 1이 "왜 하드코딩을 걷어내야 하는가"를 답하고,
축 2가 "무엇이 그 자리를 대신하는가"를 답한다.

## 1. 축 1 — 자율주행의 교훈과 Glue Code의 붕괴

### 1.1 Rule-based 스택의 한계 (자율주행이 증명한 것)

초기 자율주행 스택은 수십만 줄의 C++ 규칙 — "차선이 이러면 이렇게 꺾어라",
"무단횡단자가 나오면 이 분기를 타라" — 로 구성됐다. 이 접근은 엣지 케이스의
조합 폭발 앞에서 붕괴했고, 비디오 입력에서 조종 출력을 직접 학습하는
End-to-End 신경망으로의 전환이 폭발적 성능 향상을 가져왔다. 이는 Sutton의
*Bitter Lesson* 의 실례다: 사람이 규칙을 넣는 것보다 학습이 이긴다.

### 1.2 동일한 실수를 반복 중인 에이전트 생태계

현재 에이전트 프레임워크·MCP·CLI 파이프라인의 실상은 다음과 같다.

```python
# 현재 대부분의 에이전트 프레임워크가 하는 짓 (하드코딩 glue)
if agent_a.response == "NEED_SEARCH":
    call_mcp_search_tool()
elif agent_a.response == "CODE_GEN":
    call_mcp_code_tool()
```

라우팅 상태 공간은 (에이전트 수 × 도구 수 × 맥락)의 곱으로 폭발하므로,
손으로 쓴 분기는 경우의 수를 이길 수 없다. 도구가 10개, 100개로 늘면
glue code는 자율주행 C++ 스택과 동일한 취약성(brittleness)으로 붕괴한다.

### 1.3 HSWM의 답

HSWM은 에이전트 간 연결 경로를 사람이 코드로 지정하지 않는다. 입출력 포트
계약(port contract)만 열어두고, 협력 노드 연결망(semantic hypergraph) 자체가
학습 가능한 신경망처럼 작동한다. 어떤 도구를 부르고 어떤 에이전트에게 맥락을
넘길지를 **가중치 장($W_t$)과 영수증(receipt) 신호**가 동적으로 강화·약화시킨다.
자율주행 신경망이 도로 상황을 보고 핸들을 꺾듯, HSWM의 가중치 장이 맥락을 보고
협력 경로를 End-to-End로 스티어링한다.

## 2. 비유가 깨지는 세 지점 — 그것이 HSWM의 설계 과제다

자율주행 비유는 출발점이지 완성이 아니다. FSD식 전환의 성공 조건과 에이전트
라우팅의 조건은 세 가지가 다르고, 이 차이가 HSWM의 실제 연구 과제를 결정한다.

### 2.1 미분 불가능성 → 영수증 기반 국소 학습

운전은 연속 제어라 역전파가 통한다. 그러나 MCP 도구 호출은 **이산 선택**이라
그래디언트가 흐르지 않는다. 따라서 HSWM은 (a) MoE 게이팅·Gumbel-softmax류의
미분 가능 근사, 또는 (b) 그래디언트를 포기하고 **영수증 신호 기반의
Hebbian/Fast-Weight 국소 학습 규칙**을 요구한다. "함께 성공한 에이전트-도구
쌍의 링크를 강화한다"는 규칙이면 역전파 없이도 가중치 장이 학습된다.

### 2.2 희소·지연 보상 → 영수증 = 조밀한 credit assignment 장치

자율주행은 수백만 시간의 주행 클립이라는 조밀한 감독 신호를 가졌다. 에이전트
협업에는 그런 데이터가 없고, 워크플로우 전체가 끝나야 성공/실패 하나가 떨어지는
희소하고 지연된 보상뿐이다. 그러므로 HSWM의 영수증은 단순 로그가 아니라
**조밀한 credit assignment 장치**여야 한다. 체인 A→B→도구C의 실패에서 원인
노드를 하이퍼그래프 위에서 역추적하는 기여도 추정이 핵심 모듈이다
(PROM-16의 인과학습 층과 연결).

### 2.3 비정상성(non-stationarity) → Fast/Slow 이중 구조

도로의 물리 법칙은 바뀌지 않지만 API는 deprecate되고 에이전트는 업데이트된다.
학습된 가중치가 계속 낡아가는 환경이다. 이것이 Fast weights(세션 내 즉응
적응)와 Slow weights(장기 협력 사전분포)의 이중 구조 — 안정성-가소성 딜레마를
구조로 푸는 핵심 장치 — 의 정당성이다.

## 3. 축 2 — 주객전도: HSWM이 신경망이고 LLM은 활성화 함수다

### 3.1 뒤집힌 통념

- **세간의 구도**: LLM이라는 거대 신경망이 주체이고, 그 겉에 에이전트
  프레임워크·외부 도구(MCP, CLI, DB)를 껍데기로 붙인다. 이 관점에서는 LLM이
  멈추거나 환각하면 전체 시스템이 함께 물러나고 통제가 불가능해진다.
- **HSWM의 구도**: 진짜 신경망은 HSWM 그 자체($H_t, W_t, A_t$)이고, LLM은
  그 신경망의 노드들을 켜고 끄는 비선형 계산 소자 $f_i$일 뿐이다. 기존 ANN에서
  ReLU가 뉴런의 입출력을 변환했듯, HSWM에서는 LLM이 마이크로 노드의
  **의미적 활성화 함수**로 구동된다. LLM은 얼마든지 교체 가능한 엔진 소자이며,
  시스템의 기억·가중치·전체 인지 동역학은 거시 신경망 HSWM에 귀속된다.

### 3.2 "신경망이다"를 공학으로 만드는 동형 표

이 명제가 은유가 아니라 동형(isomorphism) 주장이 되려면, 신경망을 신경망이게
하는 성질들이 대응물을 가져야 한다.

| 신경망의 성질 | HSWM 대응물 |
|---|---|
| 가중치 결합 (synaptic weights) | $W_t$ — 에이전트/도구 간 협력 가중치 장 |
| 활성 상태 (activations) | $H_t, A_t$ — 노드별 의미 상태와 순간 활성 |
| 순방향 동역학 (forward pass) | $x_i^t, a_{\mathcal N(i)}^t$ — 하이퍼그래프 메시지 패싱 |
| 학습 규칙 (weight update) | 영수증 기반 Fast/Slow 가중치 갱신 + topology rewrite |

이 표가 성립하는 순간 "HSWM = 신경망"은 측정 가능한 연구 질문을 낳는다(§6).

## 4. 프랙탈 구조의 귀결

$f_i = \operatorname{LLM}$이라는 것은 **활성화 함수 자체가 하나의 완결된
신경망(트랜스포머)**이라는 뜻이다. HSWM은 "신경망을 뉴런으로 쓰는 신경망",
즉 프랙탈 구조다. 이 사실이 두 가지를 강제한다.

1. **거시 신경망에는 역전파가 불가능하다.** ReLU는 미분 가능하지만 LLM은 아니다.
   따라서 §2.1의 영수증 기반 Hebbian/Fast-Weight 학습은 HSWM의 선택이 아니라
   **구조적 필연**이다.
2. **포지셔닝**: 가장 가까운 기존 아키텍처는 Mixture-of-Experts다. HSWM은
   *의미 수준의 영구적·가소적 MoE* — expert가 LLM 노드이고, gating network가
   학습되는 가중치 장이며, 네트워크 전체가 태스크를 넘나들어 상태를 유지하는
   시스템으로 위치한다.

## 5. 정직한 난제 두 가지

이 관점이 만드는 새 문제를 감추지 않는다.

### 5.1 순방향 패스의 비용 = 경제학

ReLU 한 번은 무료지만 LLM 한 번은 토큰이다. HSWM에서 토폴로지 희소화와
게이팅은 미학이 아니라 **예산 배분 문제**다. "이번 스텝에 어느 서브그래프만
활성화할까"가 곧 비용 결정이며, 쉬운 노드엔 소형 로컬 모델, 어려운 노드엔
대형 모델을 배정하는 mixture-of-depths 식 라우팅이 자연스럽게 따라온다.

### 5.2 기판 표류(substrate drift)

"LLM은 교체 가능한 소자"는 절반만 맞다. 모델을 바꾸는 것은 **학습 도중
활성화 함수를 ReLU에서 tanh로 교체하는 것**과 같아, 기존 가중치 장이 부분적으로
무효화된다. 따라서 $\rho_i$(포트 계약)는 단순 인터페이스가 아니라 **모델 교체
시 가중치를 보존하는 정규화층**으로 설계되어야 하며, 모델 스왑 시 가중치
재보정(re-calibration) 절차가 HSWM 명세에 포함되어야 한다.

## 6. 정제된 테제와 검증 가능한 연구 질문

### 6.1 정제된 테제

> 하드코딩을 제로로 만드는 것이 아니다. **하드코딩은 제약 조건 — 포트 계약,
> 타입 검증, 보안 헌법, CAS/CRDT 제어 평면 — 으로만 격하시키고, 그 제약이 허용하는
> 실행 가능 집합 안의 모든 의사결정(누구와 협력할지, 어떤 도구를 부를지, 맥락을
> 어디에 저장할지)은 신경망적 가중치 장이 End-to-End로 스티어링한다.**

FSD도 순수 신경망이 아니라 rule-based 안전 봉투(safety envelope)를 얹고 있다.
검증 가능성은 계약층이 담당하고, 신경망은 계약 안에서만 동작한다 — 이 구조는
약점이 아니라 "신경망은 불투명해서 못 믿는다"는 반론에 대한 답이다.

### 6.2 Falsifiable 연구 질문

1. **경험-품질 곡선**: 라우팅 품질이 영수증 축적에 따라 단조 향상하는가?
2. **전이**: 태스크 계열 A에서 학습된 가중치가 계열 B의 초기 성능을 높이는가?
3. **절제(ablation)**: 가중치 장을 제거하면 하드코딩 라우터 기준선 수준으로
   퇴보하는가?

이 세 질문은 기존 F1–F5 게이트와 같은 장부(ledger) 절차로 사전등록·봉인되어야
하며, 이 문서 자체는 어떤 실험 결과도 주장하지 않는다.

## 7. Claims and Non-claims

- **Claim (motivation)**: 하드코딩 glue code는 조합 폭발로 붕괴하며, 학습되는
  가중치 장으로의 대체가 필요하다(§1–2).
- **Claim (identity)**: HSWM의 목표 정체성은 "LLM을 활성화 함수로 쓰는 거시
  시멘틱 신경망"이다(§3–4).
- **Non-claim (efficacy)**: 이 문서는 HSWM의 구현이 이미 그렇게 동작한다거나,
  어느 벤치마크에서 우수하다는 실험 주장을 하지 않는다. 프로그램 전체의
  `scientific_status`는 장부가 정하는 대로 `UNJUDGED`를 유지한다.
- **Non-claim (uniqueness)**: MoE·Neural Turing Machine·DNC 등과의 정량적
  선행연구 대조는 별도 문서의 과제다.

## 8. 관계 문서

- README `Core Motivation` 절 — 본 문서의 축 1 요약본 (2026-07-30)
- `CANON_DIRECTION_NEURAL_COGNITIVE_ENTITY_2026-07-23.md` — 정체성 정전
- `PROM_9_HSWM_LLM_FUNCTION_SEMANTIC_NEURAL_NETWORK_2026-07-24.md` — 최소 구현 계약
- `PROM_16_HSWM_HOLISTIC_SCIENTIFIC_ARCHITECTURE_2026-07-26.md` — 총체 과학 구조
- `research/HSWM_RESEARCH_LEDGER.v1.json` — 실험 승격의 유일한 기계 정본
