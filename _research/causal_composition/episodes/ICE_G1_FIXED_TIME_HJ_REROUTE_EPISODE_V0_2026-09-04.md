# ICE G1 fixed-time HJ reroute episode v0

> **Status:** `SECONDARY_AI_SCOPED_EXTERNAL_WORKBENCH_RESULT /
> INVENTION_CARRYING_CANDIDATE_TRANSITION /
> FROZEN_MIDPOINT_EXACT_FIXED_TIME_HJ_IDENTITY_RETIRED /
> NOT_PREREGISTERED / ICE_GATE1_OPEN_PARTIAL_PROGRESS /
> HSWM_G0_NOT_PASSED / HSWM_G1_LOCKED`
>
> **Date:** 2026-09-04
>
> **Predecessor episode:**
> [`ICE_G1_DISCRETE_GAUGE_REROUTE_EPISODE_V0_2026-09-04.md`](ICE_G1_DISCRETE_GAUGE_REROUTE_EPISODE_V0_2026-09-04.md)
>
> **Tested mechanism:**
> `FROZEN_MIDPOINT_AS_EXACT_FIXED_TIME_HJ_ELEMENT`
>
> **Selected successor discriminator:**
> `open:gate1-starobinsky-exact-gauge-preserving-element-source`, narrowed to
> one named continuum branch, a local constrained principal function,
> composition, one internal-vertex gauge-null mode, and a controlled map to
> the frozen kernel
>
> **Authority:** `SECONDARY_AI_RESEARCH_INTERPRETATION`; the source result is
> owned by the sibling ICE workbench. This projection is neither independent
> human mathematical approval nor HSWM admission, cognition, learning, or
> execution authority.
>
> **Automatic next work:** `null`

## 1. Outcome first

One precommitted ICE calculation was executed exactly once and returned:

```text
KILL_FROZEN_MIDPOINT_AS_EXACT_FIXED_TIME_HJ_ELEMENT
```

The frozen Starobinsky midpoint formula reconstructs the existing algebraic
`m=2` convention, but it is not pointwise equal to the exact fixed-time
Euclidean Hamilton principal function of the declared Hamiltonian. Its
equal-endpoint Hamilton--Jacobi residual is strictly positive at one exact
algebraic witness and at the frozen boundary witness under a 300-bit Arb
certificate.

The result does **not** reject a lapse-extremized constrained principal
function, a different perfect or improved action, a controlled deformation of
the midpoint formula, or a first-order BFV source. No BFV data, `N=0` result,
cover, end census, orientation, relative chain, global coefficient, physics
result, or TOE claim is produced. ICE Gate 1 remains open; HSWM G0 and G1 are
unchanged.

## 2. Target identity, evidence, and conceptual delta

| Layer | Bound statement |
|---|---|
| HSWM target identity | HSWM remains one token-native LLM-function macro-neural network whose evolving canonical hypergraph acts as living harness, world model, and continuous learner under outcome-bound dynamics. ICE and both repositories' graphs are bounded interfaces and projections, not HSWM cognition. |
| Prior route | Direct reuse of the local `V=0` BFV source and the naive two-local-lapse unfixing had been retired in separate scopes. The next candidate was to treat the frozen midpoint cell itself as an exact Hamilton-principal-function element. |
| New external evidence | One clean `VALID_RUN` gives 16/16 exact checks, 4/4 Arb certificates, and 5/5 theorem/scope guards. Three independent post-run AI audits recomputed both witnesses and both payload hashes and found no blocker. |
| Conceptual delta | “Use the frozen midpoint action as the exact element” is removed. The live route now requires a **different** constrained-HJ/perfect construction or a controlled deformation, with a named branch and a proof of composition and the internal gauge mode before BFV data are derived. |
| HSWM evidence ceiling | No HSWM Atom revision, schema-relative responsibility owner, Permit, readset/writeset, pre/post state digest, independent future outcome, causal credit, remove/restore intervention, or changed HSWM behavior exists. G0 remains not passed and G1 remains locked. |

This is an invention-carrying change in the **future experiment selection**,
not evidence that HSWM learned it through its own causal recurrence.

## 3. Immutable ICE ledger

The source files are retained in `/home/lagyeongjun/CD/ICE_ORCA_DRAGON`.
ICE commits remain local-only; they are not public timestamps or independent
custody.

| Object | Immutable locator or digest | Role |
|---|---|---|
| Input/runner commit | `6ed8ce69e50adace61f2fe673dc20647bac256a9` | Commits the sealed manifest and bounded runner before execution. |
| Result/report/graph commit | `4d5b18997feb6b9cce1def6c53239f0996d83031` | Retains the raw result, proof report, scoped graph contradiction, source boundaries, and refined open question. |
| Input manifest | SHA-256 `6c2ab273fd8d3c95e4b85337cf7f55c84eb961117c3db5982d65a260e4ba0696` | Freezes the fixed-time convention, candidate, witnesses, decision table, source versions, resource caps, and null outputs. |
| Runner | SHA-256 `287c142e1221ec8289f73be7e7054044773e47d6096c769c514db91f53c658ae` | Derives the exact HJ residual and performs the frozen-boundary Arb certificate. |
| Raw result | SHA-256 `8defcaaccc03dc88251758815003592016b4b4cedb538d5426289c97f4d99325` | Machine-readable source of truth. |
| Canonical JSON payload without self field | SHA-256 `7eca45eb80add06c27eef2578049e657bba2f106dd5f5b10b1f2b42f341d0550` | Internal payload-integrity identifier, not a signature, timestamp, or custody proof. |
| ICE report | SHA-256 `5d0c24b9bc37124c7c9b5477737ed4280b3c9a4fcb0b215e48d8b9b23e127579` | Human-readable derivation, witnesses, scope, route change, and review boundary. |

Observed command, exactly once:

```text
./ice run gate1_m2_starobinsky_midpoint_hj_identity_audit
```

The result records `automatic_next=null`, zero root calls, zero ODE calls,
zero numerical samples, and zero automatic descendants.

## 4. Exact fixed-time result

The declared Euclidean configuration action is

```math
I_E=2\pi^2\int d\tau\left[
\frac12G_{AB}(q)\dot q^A\dot q^B+U(q)
\right],
```

with

```math
q=(a,\phi),\qquad
G_{AB}=\mathrm{diag}(-6a,a^3),
```

```math
U(a,\phi)=-3a+a^3V(\phi),
\qquad
V(\phi)=\frac34\left(1-e^{-\sqrt{2/3}\,\phi}\right)^2.
```

Its fixed-time Hamiltonian is

```math
H_E=
-\frac{p_a^2}{24\pi^2a}
+\frac{p_\phi^2}{4\pi^2a^3}
+6\pi^2a-2\pi^2a^3V(\phi).
```

For endpoint midpoint `q_m`, increment `Delta q`, and element duration `t`,
the tested formula is

```math
S_{\mathrm{mid}}(q_0,q_1;t)
=2\pi^2\left[
\frac{G_{AB}(q_m)\Delta q^A\Delta q^B}{2t}
+tU(q_m)
\right].
```

The relation `t=T/2` with two identical algebraic copies reconstructs the
frozen `m=2` expression. It is not a sequential continuum trajectory,
composition law, or phase-space lift.

On the diagonal `q_0=q_1=q`, exact differentiation gives

```math
\partial_tS_{\mathrm{mid}}=2\pi^2U,
\qquad
\partial_{q_1^A}S_{\mathrm{mid}}=\pi^2tU_A,
```

and hence

```math
R_{\mathrm{HJ}}
=\partial_tS_{\mathrm{mid}}
+H_E\!\left(q_1,\partial_{q_1}S_{\mathrm{mid}}\right)
=\frac{\pi^2t^2}{4}D(a,\phi),
```

```math
D(a,\phi)=G^{AB}U_AU_B
=-\frac{U_a^2}{6a}+\frac{U_\phi^2}{a^3}.
```

At the exact witness

```math
a=2,qquad
\phi=\sqrt{\frac32}\log2,qquad
t=\frac12,
```

the runner obtains

```math
D=\frac{45}{64},
\qquad
R_{\mathrm{HJ}}=\frac{45\pi^2}{1024}>0.
```

At the frozen witness

```math
a=3.5668031935672753,qquad
\phi=1.0185809464006637,qquad
t=\frac25,
```

300-bit outward-rounded Arb arithmetic certifies

```math
D=2.358628592398597038799101768790179109\ldots>0,
```

```math
R_{\mathrm{HJ}}
=0.931149245442895690948698433641264018\ldots>0.
```

An exact constant-flat-metric control returns zero residual. The formal
off-diagonal counterterm candidate

```math
\Delta S(q_0,q_1;t)
=-\frac{\pi^2t^3}{12}D(q_m)
```

cancels only the displayed diagonal order-`t^2` term through its explicit
time derivative. There is no off-diagonal residual bound, convergence proof,
perfect action, or source-deformation theorem. It is a direction for the next
construction, not a result to promote.

## 5. Requested six-stage programme status

| Stage | Current status | Admission condition |
|---|---|---|
| 1. Named relative-chain theory/end model and exact joint `m=2` action | The frozen bosonic action and hypotheses-open Pham--Witten type remain recorded. No production relative-chain inhabitant or complete end model exists. | First construct a common same-model source and its coefficient/orientation line; then prove the chosen relative-chain hypotheses. |
| 2. Replacement-gauge FP/BFV source | Direct `V=0` transplant, naive lapse unfixing, and exact fixed-time midpoint equality are retired in distinct scopes. No replacement source exists. | Construct the constrained local element and controlled map below before extending it to nonminimal BFV data. |
| 3. Source and limit order including `N=0` | `BLOCKED`. The BFV multiplier locus and proper-time or element-duration divisors remain different typed objects. | A source must exist before its distributional extension and regulator order can be proved. |
| 4. `U_1/U'_1`, overlaps, basis and orientation | `BLOCKED`. There is no common source space, cover, transition map, determinant line, or orientation local system. | Build and prove the source first, then every overlap and cocycle. |
| 5. End/divisor census and candidate completeness | `OPEN`. Earlier end candidates are partial and do not form a complete census. | Prove an all-end/divisor/Stokes census and a tame thimble basis or finite-chain quasi-isomorphism. |
| 6. Independent review and preregistration | Three post-run AI audits passed; independent human mathematical ownership and independent custody are absent. | Human review must sign the complete prospective source/type/limit/cover/orientation/census/completeness packet before preregistration. |

The lateral terminal table remains unchanged:

| Terminal | Status | Reason |
|---|---|---|
| `UNIQUE_LATERAL` | `UNEVALUATED` | No common source, cover, orientation line, or global coefficient. |
| `STOKES_SPLIT` | `UNEVALUATED` | No chamber-complete Stokes transport. |
| `NO_ADMISSIBLE_LIFT` | `UNEVALUATED` | Three scoped route failures are not a nonexistence proof for every lift. |

Null values mean uncomputed, not zero.

## 6. Outcome-bound invention transition

| Transition field | Bound content |
|---|---|
| Unexpected outcome | Exact algebraic reconstruction of the frozen `m=2` convention does not make the midpoint cell an exact fixed-time Hamilton principal function; its diagonal HJ defect is explicitly nonzero. |
| Competing mechanisms | `DIRECT_V0_TRANSPLANT`, `NAIVE_MIDPOINT_TWO_LOCAL_LAPSE_UNFIXING`, and `FROZEN_MIDPOINT_AS_EXACT_FIXED_TIME_HJ_ELEMENT` are retired in distinct scopes. `CONTINUUM_FIRST_CONSTRAINED_PRINCIPAL_FUNCTION`, `CONTROLLED_HJ_DEFORMATION_OR_PERFECT_ACTION`, and `STAROBINSKY_CONSTRAINT_ADAPTED_IMPROVED_STATIC_SOURCE` remain live and unconstructed. |
| Discriminating question | For one named Starobinsky continuum branch, can a local constrained principal function be constructed with exact endpoint HJ constraints, composition, exactly one internal gauge-null mode, transverse nondegeneracy, and a controlled projection or deformation bound to the frozen kernel? |
| Changed future selection | Do not attach BFV data to the frozen midpoint by equality. Test the smaller constrained-HJ branch and controlled-map prerequisite before BFV, `N=0`, cover descent, end census, or source-to-fold transport. |

An ontology edge or another explanatory document that does not change a
prediction, competitor disposition, discriminating experiment, or future
selection remains maintenance rather than invention.

## 7. Frozen next discriminator

The next bounded packet is a **local constrained-HJ element construction or
obstruction**, not the full BFV/relative-chain stack and not another midpoint
identity test.

Before observing any new outcome, its manifest must bind:

```text
one exact Starobinsky first-order parent and Euclidean/Lorentzian/Wick convention
one named real or complex local branch beta and endpoint tube
one clock chi, with a declared FP bracket and horizon exclusion
one validated local Hamiltonian flow or boundary-value construction
caustic/conjugate-point and branch-collision predicates
endpoint HJ constraints and momentum signs
exact two-cell composition and internal momentum matching
internal gauge vector R=X_C q, H R=0, rank-one transverse condition
one explicit projection Pi_chi or deformation to the frozen midpoint kernel
a uniform neighborhood and error/remainder bound for that map
resource caps, failure predicates, source hashes, and null downstream outputs
```

For a branch-labelled constrained principal function

```math
W_\beta(q_-,q_+)
=\underset{C_V=0,\ q(\lambda_0)=q_-,\ q(\lambda_1)=q_+}
{\mathrm{ext}}
\int_{\lambda_0}^{\lambda_1}
\left(p_i\dot q^i-NC_V\right)d\lambda,
```

the packet must test, on its declared regular branch,

```math
p_+=\partial_{q_+}W_\beta,
\qquad
p_-=-\partial_{q_-}W_\beta,
```

```math
C_V\!\left(q_+,\partial_{q_+}W_\beta\right)=0,
\qquad
C_V\!\left(q_-,-\partial_{q_-}W_\beta\right)=0.
```

For

```math
S_{012}^{\mathrm{candidate}}
=W_\beta(q_0,q_1)+W_\beta(q_1,q_2),
```

internal momentum matching must imply exact composition on the declared
branch. If

```math
H_{ij}=\partial_{q_1^i}\partial_{q_1^j}
S_{012}^{\mathrm{candidate}},
\qquad
R^i=\{q_1^i,C_V\},
```

then the necessary local gauge-mode test is

```math
H_{ij}R^j=0,
\qquad
R\ne0,
\qquad
\mathrm{rank}(H)=1
```

for this two-coordinate internal vertex, together with a nonzero declared
transverse quadratic form. This is a matching/on-shell identity for the
reduced constrained element; it must not be replaced by the fixed-time HJ test
that just failed.

The map to the frozen kernel must be labelled `EQUALITY`, `DEFORMATION`, or
`APPROXIMATION`, and the latter two require a domain and bound. A bare formal
series or pointwise fit is insufficient.

The prospective terminal set is:

```text
KEEP_LOCAL_CONSTRAINED_HJ_GAUGE_MODE_AND_CONTROLLED_MAP
KILL_DECLARED_LOCAL_CONSTRAINED_HJ_CANDIDATE
NO_ADMISSIBLE_LOCAL_BRANCH_FOR_DECLARED_LIFT
CAUSTIC_OR_CLOCK_BOUNDARY_REQUIRES_REBRANCHING
INCONCLUSIVE_NO_PROMOTION
```

`KEEP` would admit only the local classical prerequisite. It would not yet
construct the nonminimal BFV source, absolute measure, `N=0` distribution,
cover, end census, orientation, relative chain, or lateral verdict.
`KILL` may retire only the manifest's declared branch/candidate. Failure of
numerical shooting or a missing rigorous enclosure is `INCONCLUSIVE`, not a
no-go. No successor runner is authorized automatically.

## 8. Standards-first graph projection

No new graph backend or HSWM-owned adapter is needed. The existing stable
profile remains sufficient:

```text
SourceArtifact
  -> usedBy -> ExactIntervalAuditActivity
  -> generated -> ScopedFixedTimeIdentityContradiction
  -> validWithin -> PointwiseDiagonalWitnessScope
  -> disposesWithinScope -> MechanismDisposition
  -> motivates -> ConstrainedHJConstructionProposal
```

RDF 1.1/N-Quads, PROV-O/PROV-DM, SHACL 2017, and JSON-LD 1.1 remain the
stable interchange/validation lane; RDFC-1.0 is used only when canonical RDF
bytes are required. RO-Crate 1.3 is post-terminal packaging, and OpenLineage
is optional run telemetry. Draft or candidate revisions remain
non-promoting.

The ICE graph records the raw result as source of truth and creates no
duplicated evidence snapshot. Validation passed with 1,484 nodes, 3,855 edges,
and 419/419 graph artifact hashes. SHACL conformance, 17/17 competency cases,
and 21/21 targeted ontology/Markdown tests passed. These are software and
projection checks, not scientific corroboration.

This HSWM episode is a loss-aware research projection. It does not create a
canonical HSWM Atom, Permit, Learn transition, or causal credit.

## 9. Independent review and preregistration gate

Three post-run read-only AI audits independently recomputed the raw and
self-excluding payload hashes, exact witness, frozen witness, interval signs,
source locators, and fixed-time/constrained claim boundary. They found no
blocker.

This is not the independent human mathematical review required before
preregistration, and the local ICE commits are not independent custody. A
human mathematical owner must review the prospective constrained-HJ packet
and its exact parent/branch/map claims before it can enter the ordered
OSF -> DSSE/in-toto -> Rekor plus RFC 3161 -> separate-account WORM -> Temporal
one-shot -> pre-pulse actor sealing -> drand -> independent custodian reveal ->
dual inspection -> sealed result/VOID -> RO-Crate/OpenLineage protocol.

The completed result is retrospective and must never be relabelled as a
prospectively preregistered occurrence.
The named services are prospective capability requirements only; no provider,
account, implementation version, or custody arrangement has been selected,
trusted, or invoked for a future occurrence.

## 10. Artifact classification

The ICE result is a material bounded negative result in the ICE workbench. Its
scientific value is one scoped identity-map retirement and a smaller next
discriminator. The ICE graph and this HSWM document are provenance/design
projections around it.

No HSWM content-addressed result receipt or `F1_R8_RESULTS_LOG.md` entry is
created. HSWM did not execute or admit a material HSWM result, and creating a
receipt would misattribute external evidence as HSWM progress.

## 11. Primary sources

- [Bahr, Dittrich, and Steinhaus, *Perfect discretization of
  reparametrization invariant path integrals*,
  arXiv:1101.4775v1](https://arxiv.org/abs/1101.4775v1) supplies the
  perfect-discretization precedent, not this Starobinsky result or a
  replacement element.
- [Dittrich and Höhn, *Constraint analysis for variational discrete systems*,
  arXiv:1303.4294v3](https://arxiv.org/abs/1303.4294v3) supplies the distinction
  between a variational discretization and proved constraint/gauge structure,
  not the repository verdict.
- [Dittrich, *From the discrete to the continuous: Towards a cylindrically
  consistent dynamics*,
  arXiv:1205.6127v1](https://arxiv.org/abs/1205.6127v1) supplies HJ/perfect and
  coarse-consistency motivation, not the local Starobinsky branch or map.
- [Johansson, *Arb: efficient arbitrary-precision midpoint-radius interval
  arithmetic*, arXiv:1611.02831v1](https://arxiv.org/abs/1611.02831v1)
  supplies outward ball semantics, not the HJ theorem or physical
  interpretation.
- [Henneaux, Teitelboim, and Vergara, *Gauge invariance for generally
  covariant systems*,
  arXiv:hep-th/9205092v1](https://arxiv.org/abs/hep-th/9205092v1) bounds the
  endpoint and gauge-transformation obligations of the prospective
  first-order parent; it does not supply the Starobinsky construction.
- [PROV-O, W3C Recommendation 30 April
  2013](https://www.w3.org/TR/2013/REC-prov-o-20130430/),
  [SHACL, W3C Recommendation 20 July
  2017](https://www.w3.org/TR/2017/REC-shacl-20170720/),
  [RDF 1.1 N-Quads, W3C Recommendation 25 February
  2014](https://www.w3.org/TR/2014/REC-n-quads-20140225/),
  [JSON-LD 1.1, W3C Recommendation 16 July
  2020](https://www.w3.org/TR/2020/REC-json-ld11-20200716/),
  [RDF Dataset Canonicalization 1.0, W3C Recommendation 21 May
  2024](https://www.w3.org/TR/2024/REC-rdf-canon-20240521/), and
  [RO-Crate 1.3](https://www.researchobject.org/ro-crate/specification/1.3/index.html)
  define the unchanged graph/provenance/validation/packaging boundaries.
