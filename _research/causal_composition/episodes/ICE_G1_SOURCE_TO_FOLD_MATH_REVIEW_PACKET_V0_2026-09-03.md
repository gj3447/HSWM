# ICE G1 source-to-fold mathematical review packet v0

> **Status:** `SECONDARY_AI_MATHEMATICAL_REVIEW_RECOMMENDATION /
> DESIGN_REVISE_WITH_NAMED_TYPE_OR_COVER_DEFECT / NOT_PREREGISTERED /
> NOT_EXECUTED / ICE_GATE1_UNCHANGED / HSWM_G0_NOT_PASSED /
> HSWM_G1_LOCKED`
>
> **Date:** 2026-09-03
>
> **Canonical blocker:**
> `open:gate1-original-cycle-signed-global-intersections`
>
> **Authority:** `SECONDARY_AI_RESEARCH_DESIGN_AND_REVIEW`; this is not the
> required decision of an independent mathematical owner.
>
> **Automatic next work:** `null`

## 1. Review answer first

The requested source-to-fold discriminator is scientifically well targeted, but
it is **not yet mathematically admissible for preregistration**. The checked-in
ICE record supplies a partial source/origin object, a partial local fold object,
and several separately scoped orientation and convergence controls. It does not
supply one common joint relative-chain object, `U1`/`U1'`, their overlaps, a
source equality through zero lapse, a common Gate-1 orientation line, a derived
finite BFV source, or a candidate-completeness certificate.

This packet therefore does four things without inventing missing evidence:

1. defines the one parameterized relative-chain **type contract** that every
   patch must inhabit;
2. defines the patch, overlap, source, limit, orientation, good-end, divisor,
   gauge/BFV, and completeness obligations;
3. gives a fail-closed decision table for `UNIQUE_LATERAL`, `STOKES_SPLIT`, and
   `NO_ADMISSIBLE_LIFT`; and
4. records the current review recommendation as
   `DESIGN_REVISE_WITH_NAMED_TYPE_OR_COVER_DEFECT`.

The recommendation is not `DESIGN_REJECTED_AS_NONDISCRIMINATING`: if repaired,
the object directly addresses the current G1 blocker. It is not
`DESIGN_ADMISSIBLE_TO_PREREGISTER`: the required type currently has no complete
instantiation.

## 2. Target identity, evidence, and conceptual delta

| Layer | Bound statement |
|---|---|
| HSWM target identity | HSWM remains one token-native LLM-function macro-neural network whose evolving canonical hypergraph is living harness, world model, and continuous learner under outcome-bound typed dynamics. This packet is a bounded research readout, not another subsystem or cognition claim. |
| Current HSWM evidence | G0 measurement integrity has not passed and G1 remains locked. A mathematically valid ICE outcome would only become an external outcome candidate; it would not itself establish an owner/Permit-valid HSWM revision or changed held-out behavior. |
| Current ICE evidence | Gate 1 is `OPEN_PARTIAL_PROGRESS`; the original joint relative cycle, complete global oriented intersection vector, physical cycle, physics claim, and TOE claim remain null. |
| Conceptual delta | Replace the informal phrase “transport from source to fold” with one dependent mathematical contract: one ambient joint type, compatible local restrictions, explicit overlap maps, ordered source limits, a common orientation line, joint admissibility, and a proof-relative finite candidate universe. |
| Present work class | `SUPPORTING_INFRASTRUCTURE / RESEARCH_DESIGN_REFINEMENT`. It makes the already selected discriminator reviewable and exposes a partition defect; it is not an invention result or G1 evidence. |

The ICE planner classified the question as a `CURRENT_BLOCKER_CANDIDATE` and
returned `AWAITING_HUMAN_REVIEW`; it granted neither execution nor core-progress
authority. No planner checkpoint is persisted by this document.

## 3. Frozen source snapshot

The frozen HSWM design source is
[`ICE_HSWM_NEXT_QUESTION_INVENTION_EPISODE_V0_2026-09-03.md`](https://github.com/gj3447/HSWM/blob/493e8173d363f1f3aa1d664ecb9334a09c75ccbf/_research/causal_composition/episodes/ICE_HSWM_NEXT_QUESTION_INVENTION_EPISODE_V0_2026-09-03.md),
SHA-256 `4376b4eb6c8af1bbf883677c325afeaabf8e4482fb8f90858c379fae813485af`,
at HSWM commit `493e8173d363f1f3aa1d664ecb9334a09c75ccbf`.

The ICE workbench was inspected at local commit
`b26901f8b64eb6a1ee4ba21f661140cf590d7a7f`. Every source below is
byte-identical at published `origin/main` commit
`f91af467bafa3cb5b3a399627ee6715084f14dff`, so links and hashes bind a public
immutable source. Local-only output and later sidecars are excluded.

| Source | SHA-256 | Review use |
|---|---|---|
| [`GATE1_AFFINE_PHASE_BAND_RELATIVE_CLASS.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/GATE1_AFFINE_PHASE_BAND_RELATIVE_CLASS.md) | `06108ec01c04eb542a0a3051defe2fa6cf0817c7e0d6299c3b5f133931860a6c` | One reduced fixed-`a` scalar class, strict good-band conditions, and missing source selection. |
| [`GATE1_SCALAR_SOURCE_LINK.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/GATE1_SCALAR_SOURCE_LINK.md) | `1bad856817974749633216b013f681bd2c2891a3d06c6b453ba945a942b36d68` | Ordered scalar source, `i epsilon`, nonzero-lapse distribution, and scalar orientation `+1`. |
| [`PHASE32_BELOW_ORIGIN_LAPSE_INTERSECTION.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/PHASE32_BELOW_ORIGIN_LAPSE_INTERSECTION.md) | `cb5f112eaa5f73460dd97994583233306fe5bda7331868d49a703c568f41279f` | Below-origin cap, projected coordinate sign, nonuniform zero-lapse limit, and missing joint orientation. |
| [`PHASE36_AIRY_GAUSS_MANIN_CONNECTION.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/PHASE36_AIRY_GAUSS_MANIN_CONNECTION.md) | `83eb4e1abfca5db76efdb22451c0f655a39ae73085723d60b06379f4dd117db7` | Local Airy bases, cycle/dual transitions, and two surviving root-sheet laterals. |
| [`PHASE38_JOINT_CYCLE_IDENTIFIABILITY.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/PHASE38_JOINT_CYCLE_IDENTIFIABILITY.md) | `c275687bd73e7e18442e73619f75585f2cfdbab4cf80d9c7c2a65e3a2ecc52cf` | Required joint completion object, type-error guard, bounded end ledger, and next exact construction. |
| [`GATE1_BOSONIC_CANONICAL_SOURCE_PUSHFORWARD.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/GATE1_BOSONIC_CANONICAL_SOURCE_PUSHFORWARD.md) | `31c267819fae11c089de5b0ba833bf093b25965ad5ecf369703ef564934f79b3` | Common-`i0` obstruction on unchanged real momenta and need for a conformal/gauge prescription plus determinant-line glue. |
| [`GATE1_TRACE_GAUGE_FP_ADMISSIBILITY.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/cpt_temporal_folded_susy/GATE1_TRACE_GAUGE_FP_ADMISSIBILITY.md) | `6eda1a134f145242587dec674e40f7659bbc4deb16d9d22095358a3cf9c46188` | Local FP transversality, killed append shortcut, and uncomputed replacement-gauge obligation. |
| [`ICE_TOE_CRITICAL_PATH_ROUTING_2026-09-01.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/docs/decisions/ICE_TOE_CRITICAL_PATH_ROUTING_2026-09-01.md) | `d037a45a87201118f25fa8c3e6cbe3f2f8aeebd1eb7cbb424c17e1ebbf140569` | Active G1 blocker and claim dependency. |
| [`ICE_CHOICE_INVARIANCE_CROSS_DOMAIN_PROMOTION_2026-09-02.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/docs/decisions/ICE_CHOICE_INVARIANCE_CROSS_DOMAIN_PROMOTION_2026-09-02.md) | `a73a26497e56923b2d86c458340c549a3ccfdea294c58058f967d1961768b40e` | Allowed-choice family, invariant/null condition, independent checks, and non-promotion boundary. |
| [`ICE_ORDERED_FIVE_GATE_PROGRAMME_2026-08-20.md`](https://github.com/gj3447/ICE_ORCA_DRAGON/blob/f91af467bafa3cb5b3a399627ee6715084f14dff/docs/decisions/ICE_ORDERED_FIVE_GATE_PROGRAMME_2026-08-20.md) | `6ea9b85d818fd8c71a312b4b48c519b708154918677e77063a10da06cbf0a35a` | Historical typed joint-cycle target promoted into current navigation by the active critical-path decision. |

The last source is a historical map, not current execution authority. The active
critical-path decision reuses its dependency information without reactivating
its old workflow.

## 4. Same relative-chain type

### 4.1 Parameterized type schema

The ICE sources do not choose a precise chain theory for noncompact integration
cycles. Before the following schema becomes a mathematical type, the protocol
must name either a compactified relative singular-chain model or a precise
locally finite/rapid-decay chain complex, including its coefficient ring,
support condition, boundary operator, divisor behavior, and comparison maps.
Freeze that choice and one mathematical environment record before constructing
a candidate:

```math
\mathfrak F=
(m,I^{\mathrm{joint}}_m,X_{\mathrm{gf}},d,\mathcal K_{\mathrm{chain}},
\mathcal E_{\infty},D_{\mathrm{sing}},B_{\mathrm{good}},
\mathcal L_{\mathrm{G1}},
\Gamma_{\mathrm{src}},\mathcal R,\prec_{\mathrm{lim}},\sim_{\mathrm{adm}}).
```

Its fields mean:

| Field | Required meaning |
|---|---|
| `m` | Frozen discretization; this episode proposes exactly `m = 2` and makes no `m`-continuum claim. |
| `I_joint_m` | One explicit finite-cutoff holomorphic joint action containing every even integration variable used from source to fold. A scalar ablation or a sampled continuum BVP is not this field. |
| `X_gf` | The complexified, gauge-fixed even integration space with endpoint polarization and the declared fundamental region/root sheets. Its lapse, fields, momenta, and remaining gauge/modulus coordinates are explicit. |
| `d` | The middle real chain dimension, fixed by `dim_C X_gf`; every local chain has this same dimension. |
| `K_chain` | One named chain theory: for example, relative singular chains on a specified compactification, or a specified locally finite/rapid-decay complex. The coefficient ring, supports, boundary map, and notion of chain homotopy are part of the choice. |
| `E_infinity` | The exact end model used by `K_chain`: compactification/real oriented blow-up and boundary strata, or the equivalent rapid-decay support data. It fixes what it means for a chain to end at infinity or approach a divisor. |
| `D_sing` | Removed singular divisor, with every component and multiplicity named. The simple fold discriminant is handled by `Uf`; it may not be silently treated as an ordinary removable singularity. |
| `B_good` | An actual subspace/stratum or rapid-decay support condition in `E_infinity`, consisting only of proved uniformly decaying ends for the `exp(-S)` or equivalent frozen convention. A numerical box exit and an oscillatory boundary are not members. |
| `L_G1` | One coefficient/orientation local system carrying source orientation, chain orientation, Jacobian/Maslov data, and the finite gauge/FP/BFV orientation needed at Gate 1. Odd ghost variables are represented through this line data rather than pretended to be ordinary topological coordinates. The full physical fermion/Pfaffian/Pin line remains the downstream Gate-3 object. |
| `Gamma_src` | The ordered original source chain or distribution, including lapse range, integration order, endpoint state, normalization, and side prescription. |
| `R` | The complete regulator vector: at minimum field cutoff, lapse test support, origin-cap radius, `i epsilon`, scalar-tail parameter, fold radius, gauge regulator, and any spectral cutoff. |
| `limit order` | A total or explicitly justified partial order on removal of those regulators. Unlisted interchange is forbidden. |
| `admissible equivalence` | Source-preserving relative homotopy, allowed cover refinement, gauge equivalence, and coordinate/basis re-expression, with their orientation-line action. It does not identify physically distinct boundary states by default. |

Only after `K_chain` and `E_infinity` are supplied, denote the selected shared
chain complex and its homology by

```math
\mathrm{RC}_{\mathcal K}(\mathfrak F)
=\mathcal K_{\bullet}(X_{\mathrm{gf}}\setminus D_{\mathrm{sing}},
                      \mathcal E_{\infty},B_{\mathrm{good}};
                      \mathcal L_{\mathrm{G1}}),
\qquad
\mathrm{RH}_{\mathcal K}(\mathfrak F)
=H_d(\mathrm{RC}_{\mathcal K}(\mathfrak F)).
```

For an ordinary compactified relative-chain choice, the first expression must
specialize to a fully named pair such as

```math
C_{\bullet}(\overline X_{\mathrm{gf}}\setminus\overline D_{\mathrm{sing}},
            B_{\mathrm{good}};\mathcal L_{\mathrm{G1}}).
```

A rapid-decay or locally finite choice must instead supply its own exact
complex; this packet does not declare the displayed ordinary model equivalent
to those alternatives.

This refines, without claiming to instantiate, the recorded Gate-1 target

```math
\Gamma_{\mathrm{original}}^{\mathrm{joint}}
\in H_{\mathrm{rel}}(
\mathcal X_{\mathrm{lapse}}\times
\mathcal X_{\mathrm{fields}}\times
\mathcal X_{\mathrm{gauge}};\mathcal X_{\mathrm{bad}}).
```

Every local datum must be a restriction of the same dependent type:

```math
\Gamma_i\in Z_d(\mathrm{RC}_{\mathcal K}(\mathfrak F|_{U_i})),
\qquad i\in\{0,1,1',f\}.
```

A root label, lapse-plane curve, scalar tail, Airy solution, relative cycle,
dual cycle, and determinant/Pfaffian line are not mutually substitutable. A
type fingerprint must bind the canonical serialization of `F`; all four
patches and every overlap map must cite the same fingerprint.

### 4.2 Chain-level equality on overlaps

For `O_ij = U_i intersect U_j`, an overlap transition must act on both the
chain and its coefficient line:

```math
g_{ij}:\mathrm{RC}_{\mathcal K}(\mathfrak F|_{O_{ij}})
\longrightarrow\mathrm{RC}_{\mathcal K}(\mathfrak F|_{O_{ij}}).
```

Compatibility is not coordinate proximity. It requires an explicit relative
chain homotopy witness:

```math
\Gamma_j-g_{ij}\Gamma_i
=\partial B_{ij}+b_{ij},
\qquad b_{ij}\subset B_{\mathrm{good}},
```

with the induced orientation-line map recorded. Wherever triple overlaps are
nonempty, the transitions must satisfy

```math
g_{ii}=1,
\qquad g_{ji}=g_{ij}^{-1},
\qquad g_{jk}g_{ij}=g_{ik}.
```

An equality that exists only after dropping orientation, source normalization,
or gauge data is a failed overlap, not a valid transport.

### 4.3 Current inhabitance audit

| Required field | Current record | Review status |
|---|---|---|
| Explicit finite joint action and integration space | A scalar fixed-`a` action, a finite bosonic canonical control, and sampled continuum BVP data exist in different scopes. | `MISSING_COMMON_OBJECT` |
| Source chain through zero lapse | Nonzero-lapse scalar equality exists only on tests supported away from `N = 0`. | `OPEN` |
| Gate-1 coefficient/orientation local system | Scalar `+1`, a projected lapse sign, local Airy bases, and finite relative signs exist separately. | `MISSING_COMMON_TRIVIALIZATION` |
| Derived gauge/BFV source | A local trace-gauge chart exists, but appending it to the existing source is killed; replacement gauges are uncomputed. | `MISSING` |
| Relative boundary and complete divisor | Several scalar ends and local discriminants are classified, but mixed ends, box exits, zero lapse, other sheets, and regulator removal remain open. | `INCOMPLETE` |

Therefore `RC_K(F)` is a proposed dependent **type schema**, not yet a selected
chain theory. The current evidence supplies neither `K_chain`/`E_infinity`, a
complete `F`, nor an inhabitant. Failure to inhabit an undefined type is
`INCONCLUSIVE`; it is not `NO_ADMISSIBLE_LIFT`.

## 5. `U0`, `U1`, `U1'`, `Uf`, and overlaps

For a future completed `F`, let `pi_T : X_gf -> B_T` be its lapse-coordinate
projection. Each `U_i` must be an open subset of the declared
source-to-fold corridor in `B_T`, and each chain patch lives on
`pi_T^{-1}(U_i)`. A sampled curve is only a centerline; an open tubular domain,
its width, sheets, excluded set, and boundary must also be specified.

| Patch | Required domain and role | Existing anchor | Current status |
|---|---|---|---|
| `U0` | An open tube containing the declared original source arms and one regulated origin bypass, with source distribution, orientation, and cap/arm gluing. | The lower full-line bypass maps to a right `T` semicircle, with recorded caps no larger than `abs(T) = 0.1`. | `PARTIAL_CENTERLINE_ONLY` |
| `U1` | One open, divisor-free, non-Stokes intermediate corridor that carries a same-typed joint chain from `U0` to `Uf`, including full tangent/orientation transport. | Reduced local BVP/stationary-family samples occur from the origin-side region toward the fold; they do not establish a connecting joint corridor, gradient-flow manifold, or chain patch. | `UNDEFINED` |
| `U1'` | A genuinely independent admissible corridor or cover/refinement, not the same tracker with a renamed flag. It must start from the same source restriction and end in the same final trivialization. | No source-to-fold same-typed second corridor is recorded. | `UNDEFINED` |
| `Uf` | An open simple-fold neighborhood with a certified local Airy cycle basis, two lateral sectors, their relative-cycle transitions, and a Gate-1 orientation-line trivialization. Regular hard CFU coefficients remain a G2 interface, not a G1 admission condition. | Local semicircles at radii `2e-4`, `1e-3`, and `5e-3` around `T_c = 9.788625568081242` carry root and local Airy data. | `PARTIAL_LOCAL_DATA_ONLY` |

The required nerve is

```text
U0 -- O01  -- U1  -- O1f  -- Uf
 |                              |
 +-- O01' -- U1' -- O1'f ------+
```

At minimum, freeze and prove the following:

| Overlap | Mandatory predicate |
|---|---|
| `O01` | Nonempty open overlap; `U0` source restriction and `U1` incoming restriction are equal as oriented relative chains under `g_01`. |
| `O01'` | Same requirement for `U1'`, using the identical source distribution and budget. |
| `O1f` | `U1` reaches the certified fold neighborhood without an unclassified divisor, end, sheet change, or orientation zero. |
| `O1'f` | Same requirement for `U1'`. |
| `O11'` | If nonempty, bind the direct transition and all triple-overlap cocycles. If empty, state the topological reason and compare both paths only after transport to one common `Uf` trivialization. |
| `O0f` | Declared empty in this cover: the recorded origin caps and fold neighborhood are disjoint. No direct transition may be inferred. |

The two path transports are

```math
\mathcal T_P=g_{1f}g_{01},
\qquad
\mathcal T_Q=g_{1'f}g_{01'}.
```

They are comparable only on the same source class and in one common ordered
`Uf` basis with the same orientation line. Different code paths are not
independent if they share the same unverified overlap or root-label mutation.
The current record defines none of `g_01`, `g_01'`, `g_1f`, or `g_1'f` on a
joint relative-chain type.

## 6. Source, `i epsilon`, regulators, and limit order

### 6.1 What the existing scalar control actually fixes

The bounded scalar control freezes

```math
z=N-i\epsilon,
\qquad T=iz,
\qquad \epsilon>0,
```

and the ordered real measure

```math
\frac{dp_0\wedge dp_1\wedge du}{(2\pi\hbar)^2},
\qquad p_0\ \mathrm{then}\ p_1\ \mathrm{then}\ u.
```

For the declared scalar family

```math
\Gamma_\lambda:\quad q=u+i\lambda\psi/\kappa,
\qquad 0<\lambda\leq1,
```

the proved order is:

```text
integrate p0, then p1, then the scalar variable
→ remove the scalar field cutoff at fixed epsilon > 0
→ take epsilon down to 0 only after pairing with tests supported away from N = 0
→ take lambda down to 0 on those punctured-arm distributions
```

That proof does not permit tests crossing zero lapse, does not select
`lambda = 1`, and does not justify interchanging the field, lapse, cap, gauge,
or fold limits.

### 6.2 Required joint freeze

The future protocol must serialize one regulator vector, for example

```math
\mathcal R=(L_q,R_N,r_0,\epsilon,\lambda,\rho_f,
\Lambda_{\mathrm{gauge}},\Lambda_{\mathrm{spec}}),
```

with an exact semantic definition for every component. It must then choose one
declared order or prove a commuting square for every permitted interchange.
The following decisions cannot remain implicit:

| Decision | Required freeze | Current evidence |
|---|---|---|
| Source integration order | All lapse, field, momentum, multiplier, and ghost/BFV integrations or reductions; endpoint polarization and normalization. | Only bounded scalar and finite bosonic orders are explicit. |
| Lapse side | Lower, upper, or two separately declared hypotheses; relation between `N - i epsilon` and the finite origin cap. | Lower scalar control exists; physical below-versus-above choice is open. |
| Zero-lapse test space | Exact test-function space crossing `N = 0`, contact-term convention, and distributional equality. | Open. Existing equality is on a punctured support. |
| Origin limit | Whether `r_0` is fixed, tends to zero before/after `epsilon`, or is tied by a named scaling law. | Nonuniformity is recorded; no order is proved. |
| Field/good-end limit | Cutoff removal with a uniform all-end bound in the joint space. | Proved only for a restricted fixed-`a` scalar subfamily. |
| Fold limit | Fixed certified finite `rho_f`, or a separately proved `rho_f -> 0` limit with error control. | Three finite radii exist; no error-certified zero-radius theorem. |
| Gauge/spectral removal | Regulator and zero-mode removal after a derived BFV source, with stability criterion. | Undefined. |

The source-compatibility predicate must be distributional and ordered. For the
full declared test space `T_src`, it has the form

```math
\mathrm{Src}(c)=1
\quad\Longleftrightarrow\quad
\lim_{\lambda\downarrow0}
\lim_{\epsilon\downarrow0}
\lim_{L_q\to\infty}
\langle\mathcal I_c(L_q,\epsilon,\lambda),f\rangle
=\langle\mathcal I_{\mathrm{src}},f\rangle
\quad\mathrm{for\ every}\ f\in\mathcal T_{\mathrm{src}},
```

modified only to reflect the actually frozen total order. If `T_src` includes
zero lapse, the current scalar theorem is insufficient. Pointwise agreement or
agreement on punctured tests cannot be relabeled as full source equality.

## 7. Orientation and basis transition

### 7.1 One orientation record

Each candidate must carry one orientation record

```math
\mathfrak o_c=(o_{\mathrm{ambient}},o_{\mathrm{source}},o_{\mathrm{chain}},
o_{\mathrm{momentum}},o_{\mathrm{field}},o_{\mathrm{FP/BFV}},
o_{\mathrm{det/Maslov}},\{o(g_{ij})\}).
```

Every sign must state whether it is a coordinate sign, source-measure sign,
relative-chain orientation, determinant-line transition, or physical
intersection coefficient. Multiplying signs from incompatible lines is a type
error.

The current local records do **not** form this tuple:

- the scalar source-link orientation ratio is `+1` only for its declared
  scalar block;
- the Phase-32 `+1` is a projected lapse-base coordinate sign under stipulated
  local orientations;
- comparison with a separately normalized negative-real Fresnel cycle requires
  an additional `m_- = -1`, but that transition was not derived as a Maslov or
  BFV orientation;
- the finite bosonic branch has its own momentum and configuration glue ledger;
- the fold half-phase retains unresolved signs; and
- the full Gate-1 joint orientation remains null.

### 7.2 Frozen local fold algebra

In the Phase-36 ordered cycle bases, the exact local relation is

```math
\boldsymbol\Gamma_{\mathrm{in}}
=G\boldsymbol\Gamma_{\mathrm{out}},
\qquad
G=\begin{pmatrix}-1&-1\\0&1\end{pmatrix},
\qquad G^{-1}=G.
```

Chain coefficients and formal dual bases therefore transform as

```math
c_{\mathrm{out}}=G^T c_{\mathrm{in}},
\qquad
\boldsymbol K_{\mathrm{in}}=G^{-T}\boldsymbol K_{\mathrm{out}},
\qquad
G^{-T}=\begin{pmatrix}-1&0\\-1&1\end{pmatrix}.
```

The enhanced lateral basis uses

```math
S_{\downarrow}=\begin{pmatrix}1&0\\1&1\end{pmatrix},
\qquad
S_{\uparrow}=S_{\downarrow}^{-T}
=\begin{pmatrix}1&-1\\0&1\end{pmatrix}.
```

The displayed `G` has the same entries in the separately ordered CW and CCW
bases, but their companion cycles and geometric first outgoing arms differ.
It is not a common physical transport merely because the arrays agree. These
are local cycle/dual basis changes. The numerical root permutation

```math
P=\begin{pmatrix}0&1\\1&0\end{pmatrix}
```

acts on the BVP root fiber and may not replace `G`, `G^T`, or a physical
intersection map. A future atlas must transport the source class into one
common final trivialization and apply every basis change together with its
orientation-line action.

### 7.3 The already visible partition hazard

Conditional on an independently derived incoming class represented by
`c_in = (1,0)^T`, the correct local map yields

```math
G^T\begin{pmatrix}1\\0\end{pmatrix}
=\begin{pmatrix}-1\\-1\end{pmatrix}.
```

This is a fixed two-arm combination in the declared local basis. It is neither
automatically one unique lateral nor automatically two distinct
source-compatible Stokes lifts. The verdict partition must therefore send a
single admissible multi-arm class to `INCONCLUSIVE / PARTITION_DEFECT` unless a
fourth mechanism is preregistered. Forcing it into `STOKES_SPLIT` would confuse
a basis representation with two independently sourced lateral limits.

## 8. Good-end and divisor conditions

### 8.1 Uniform relative-good condition

For the frozen `exp(-S)` convention, a noncompact end `E` of candidate `c` is
admitted to `B_good` only if decay is uniform over every frozen nuisance,
overlap, and regulator range retained at that stage:

```math
\lim_{M\to\infty}
\inf_{r\in\mathcal R_{\mathrm{frozen}}}
\inf_{x\in E(c,r),\ \lVert x\rVert\geq M}
\mathrm{Re}\,S_r(x)=+\infty.
```

Pointwise decay at selected samples is insufficient. A chain with any
unclassified noncompact end is not admissible.

The existing fixed-`a` scalar theorem proves, only in its declared horizontal
affine class, the strict principal band

```math
|\psi-\kappa b(\psi)|<\frac{\pi}{2},
\qquad
b(\pi/2)>0,
\qquad
b(-\pi/2)<0.
```

The literal real-field source arms correspond to the boundary value
`lambda = 0` and have `Re S_2 = 0`; they are oscillatory boundaries, not
relative-good ends. The phase-locked representative is maximum damping, not a
source-selection theorem.

### 8.2 Divisor ledger

Before preregistration, divide the exceptional set into typed components:

| Component | Required treatment |
|---|---|
| `D_lapse` | `T = 0` or the equivalent zero-lapse locus. Avoid at finite regulator and give a distributional cap limit; never count endpoint contact as an ordinary transverse intersection. |
| `D_scale` | `A = 0`, `a_1 = 0`, and any action/measure pole created by complex scale continuation. Prove positive separation or a controlled local model. |
| `D_FP` | Gauge horizon `D_L = 0` and all residual/zero-mode degeneracies. A local chart may not cross it without a second chart and a proved transition/fundamental region. |
| `D_fold` | The intended simple fold/discriminant. Cover it only inside `Uf` with certified rank-one degeneracy, transverse unfolding, and the local Airy relative-cycle relation. Other Hessian/Jacobi zeros are unclassified divisors. |
| `D_orientation` | Zeros where the bosonic/gauge-reduced Gate-1 orientation line ceases to be nondegenerate or trivializable. Record multiplicity and side; a sampled nonzero value is not a global exclusion. |
| `D_sheet/Stokes` | Sheet branch loci and Stokes hypersurfaces. State whether they are avoided, crossed with a named jump, or used as the two declared lateral limits. |

For every component that must be avoided, require a certified positive
separation at finite regulator and a proof that the limiting chain does not
acquire an undeclared crossing. The current `SINGULAR_ENDPOINT_UNRESOLVED` and
`BOX_EXIT_UNRESOLVED` records are not good ends and cannot support a completeness
claim.

The regular hard determinant quotient, even/odd CFU coefficients, and physical
uniform kernel are tagged `G2_INTERFACE_NOT_G1_ADMISSIBILITY`. They may be
computed conditionally in parallel, but their absence or degeneration cannot
be used as a Gate-1 rejection witness or to decide this source-to-fold verdict.

## 9. Gauge/BFV admissibility

The Gate-1 predicate is deliberately narrower than the downstream physical
fermion/Pfaffian/Pin construction, but it must be stronger than a local FP
number. Define

```math
\mathrm{GaugeOK}(c)=
\mathrm{DerivedSource}
\land\mathrm{ConstraintGaugePair}
\land\mathrm{Transverse}
\land\mathrm{FundamentalRegion}
\land\mathrm{ZeroModesResolved}
\land\mathrm{EndpointCompatible}
\land\mathrm{BFVClosed}
\land\mathrm{MeasureOriented}
\land\mathrm{GaugeStable}.
```

Its obligations are:

| Predicate | Acceptance condition |
|---|---|
| `DerivedSource` | Start from local lapse/constraint variables and derive the reduced finite source. Appending `delta(chi) Delta_FP` to the unchanged proper-time/fixed-endpoint source is forbidden by the existing audit. |
| `ConstraintGaugePair` | State the constraint, gauge fermion or equivalent finite gauge condition, canonical chart, boundary action, endpoint states, and induced measure. |
| `Transverse` | The FP operator is nonzero on every admitted simple-root chart; for the trace control this includes `D_L != 0`. |
| `FundamentalRegion` | Specify which root sheet/orbit representative is counted and how charts glue across horizons; local nonvanishing alone does not exclude Gribov copies. |
| `ZeroModesResolved` | Treat the global lapse modulus, residual gauge modes, ghosts, collective coordinates, and regulator zero modes without silent deletion. |
| `EndpointCompatible` | The endpoint polarization and boundary Legendre term belong to the chosen gauge representation and reproduce its own constraint projector. |
| `BFVClosed` | The finite chain/measure is BRST/BFV closed relative to the declared boundary, or an exactly stated finite surrogate theorem proves the needed closure. |
| `MeasureOriented` | The FP/BFV determinant line and its overlap transitions are nondegenerate and included in `L_G1`. |
| `GaugeStable` | The resulting source class is equal under every preregistered admissible gauge/chart refinement, or the difference is a declared physical branch rather than hidden as invariance. |

The present trace-gauge calculation establishes a local chart on
`a > 0`, `C_L = 0`, `D_L != 0`, but it also kills reuse of the unchanged
proper-time `m = 2` source by naive append. Its improved-static and
time-dependent replacement sources are uncomputed. Consequently
`GaugeOK` is currently **undefined**, not false for an exhaustively tested
candidate. That distinction blocks `NO_ADMISSIBLE_LIFT`.

## 10. Candidate-set completeness

### 10.1 Candidate record

Every declared candidate must contain

```text
candidate_uid
type_fingerprint
source restriction and ordered-limit witness
U0/U1/U1'/Uf local chain sections
all nonempty overlap maps and chain-homotopy witnesses
good-end and divisor ledger
sheet/Stokes chamber and lateral side
orientation/BFV line and transitions
common-Uf arm vector
allowed-choice stability results
one principal rejection reason, if rejected
```

Let `Pre(F)` be the pre-admissibility universe of all well-typed,
source-anchored local-section tuples generated by the exact finite atlas
grammar. `AtlasScope` must freeze that grammar's patch support, coefficient
lattice or bounds, sheets, lateral/Stokes labels, regulator family, and source
anchor. It is independent of every final admissibility check. Let `C_decl` be a
finite set of representatives proposed to cover `Pre(F)`.

For records whose type and overlaps pass, define the partial realization map

```math
\mathrm{Glue}:\{c\in\mathrm{Pre}(\mathfrak F):
\mathrm{TypeOK}(c)\land\mathrm{CoverOK}(c)\}
\longrightarrow Z_d(\mathrm{RC}_{\mathcal K}(\mathfrak F)).
```

The map must be constructed from the local sections and chain-homotopy
witnesses; merely concatenating coordinates is not gluing. Define the joint
admissibility predicate

```math
\mathrm{Adm}(c)=
\mathrm{TypeOK}(c)
\land\mathrm{Src}(c)
\land\mathrm{CoverOK}(c)
\land\mathrm{GoodEndOK}(c)
\land\mathrm{DivisorOK}(c)
\land\mathrm{OrientationOK}(c)
\land\mathrm{GaugeOK}(c)
\land\mathrm{StabilityOK}(c).
```

The allowed equivalence relation may include source-preserving relative
homotopy, certified cover refinement, gauge equivalence inside the same
fundamental region, and `GL(Z)` basis re-expression with the induced coefficient
and orientation transforms. A general `GL(Z)` change is bookkeeping on the
abstract class: it cannot decide how many geometric fold rays are occupied.
Every arm vector must first be pulled back to the frozen sector-labelled
`(Gamma_U,Gamma_L)` basis. Without such a pullback, only signed ray-preserving
relabelings are allowed in the arm-support classifier. The relation does
**not** identify by default:

- orientation reversal that reverses the source;
- complex conjugation that exchanges independently declared ket/bra sources;
- a different self-adjoint extension, endpoint state, or Stokes chamber;
- two vectors made equal only by dropping normalization or line data.

Define

```math
\mathcal V=
\{[\mathrm{Glue}(c)]_{\sim_{\mathrm{adm}}}:
c\in\mathcal C_{\mathrm{decl}},\ \mathrm{Adm}(c)=1\}.
```

### 10.2 Completeness certificate

`NO_ADMISSIBLE_LIFT` first requires a non-vacuous coverage certificate for the
pre-admissibility universe:

```math
\mathrm{Pre}(\mathfrak F)\ne\varnothing,
\qquad
\forall p\in\mathrm{Pre}(\mathfrak F),
\qquad
\exists c\in\mathcal C_{\mathrm{decl}}:
p\sim_{\mathrm{pre}}c.
```

`Pre(F)`, `AtlasScope`, and `~pre` must be defined without `Adm`, `GaugeOK`, a
successful glue, or any other final acceptance conjunct. Otherwise an empty or
failed candidate family could satisfy the implication vacuously. After
coverage, every retained equivalence class must receive exactly one proved
class-wide obstruction or an admissible realization.

Quotienting is permitted only with a congruence certificate. For every
`p ~pre c`, it must prove that the transformation preserves the truth value of
every final predicate and, whenever gluing succeeds, the realized abstract
class:

```math
p\sim_{\mathrm{pre}}c
\Longrightarrow
\bigwedge_{A\in\mathcal A} A(p)=A(c),
\qquad
[\mathrm{Glue}(p)]=[\mathrm{Glue}(c)],
```

where

```math
\mathcal A=\{\mathrm{Src},\mathrm{CoverOK},\mathrm{GoodEndOK},
\mathrm{DivisorOK},\mathrm{OrientationOK},\mathrm{GaugeOK},
\mathrm{StabilityOK}\}.
```

The second equality is required only on the domain of `Glue`. If this
congruence cannot be proved, `~pre` may not collapse the records: the coverage
certificate must retain them separately. A rejection witness must therefore be
class-wide and invariant, not merely observed on the chosen representative.

Acceptable certificate forms are limited to:

1. an analytic classification of the relevant relative group, source boundary
   constraint, sheets, and allowed transitions that proves the quotient finite;
   or
2. a finite chain-complex/CW model with a proved scope-preserving isomorphism to
   the analytic atlas, followed by exact enumeration of its source-compatible
   classes.

A sampled root list, a solver that found no path, a finite search budget, a
single ansatz family, or an unclassified box exit is not a completeness
certificate. The existing fixed-`a` affine classification is complete only for
its strict horizontal scalar-tail family; it cannot certify the full joint
candidate universe.

## 11. Verdict table

### 11.1 Common prerequisites

No scientific verdict is evaluated until all of the following are true:

```text
P0  source snapshot and all mathematical choices were frozen before outcome
P1  F includes one named chain theory/end model and all patch data share its fingerprint
P2  U0/U1/U1'/Uf form the declared cover; overlap/cocycle predicates are complete and evaluated per candidate
P3  the full source test space, limit order, and equality predicate are defined and evaluated per candidate
P4  good-end, divisor, orientation, and GaugeOK predicates are complete and evaluated per candidate
P5  C_decl has an accepted completeness certificate
P6  P and Q are independently reconstructed in one common Uf trivialization
P7  allowed-choice family and error/interval criteria are frozen and evaluated per candidate
```

For each admissible class, let

```math
v_f([c])=(c_U,c_L)\in\mathbb Z^2
```

be its oriented arm vector after pullback to the frozen, geometrically
sector-labelled `(Gamma_U,Gamma_L)` basis. Coordinate changes must transform the
vector and basis together; equality is equality of the abstract oriented class,
not raw array equality in unrelated bases. A general `GL(2,Z)` coordinate
change cannot turn multi-arm support into a `UNIQUE_LATERAL` result.

### 11.2 Mutually exclusive decision rules

| Output | Issue only when all listed conditions hold | Exact bounded inference | Changed next selection |
|---|---|---|---|
| `UNIQUE_LATERAL` | `P0`–`P7`; `V` contains exactly one source-compatible equivalence class; both independent transports yield the same class; its source-normalized `v_f` has support on exactly one of `U` or `L`; every apparent second candidate is proved equivalent or inadmissible; stability holds under all allowed changes. | Exactly one lateral survives in the declared finite atlas and source/gauge/regulator scope. | Select the complete saddle/sheet/singularity/good-end census for that class; do not infer a global coefficient. |
| `STOKES_SPLIT` | `P0`–`P7`; `V` is exactly the preregistered lateral pair or named finite Stokes multiplet, with no residual class; every member comes from the same frozen source and passes `Adm`; after transport to one common chamber the declared jumps are nonzero elements of the same `RH_K(F)`; no jump is removed by an allowed basis, gauge, cap, orientation, or regulator change; both routes reproduce the multiplet independently. | A stable same-typed lateral/Stokes ambiguity survives in this atlas. | Stop accumulating local saddles; request an additional physical source principle or retain explicitly conditional branches. |
| `NO_ADMISSIBLE_LIFT` | `P0`–`P7`; the non-vacuous coverage and congruence certificates are accepted; `V` is empty; every `~pre` class has exactly one proved class-wide, unavoidable `SOURCE_MISMATCH`, `OVERLAP_OBSTRUCTION`, `BAD_END`, `DIVISOR_OBSTRUCTION`, `ORIENTATION_OBSTRUCTION`, `GAUGE_BFV_OBSTRUCTION`, or `ALLOWED_CHOICE_INSTABILITY` witness that survives every allowed repair/equivalence. An unknown check, representative-only failure, or solver failure is not a witness. | No lift exists only in the frozen finite atlas, action, source, gauge, and regulator scope. | Retire only the scalar-tail-as-original-cycle mechanism in that scope; redirect to a replacement source/BFV construction or a scoped no-go question. |
| `INCONCLUSIVE` | `P0` holds, but any of `P1`–`P7` is unresolved without an accepted no-lift witness; or `V` contains an outcome outside the three-way partition, including one zero-fold-support class, one fixed multi-arm class, a Stokes pair plus a residual class, or multiple non-Stokes classes. | The instrument or mechanism partition is insufficient. This is not evidence for joint obstruction. | Repair only the named type, cover, source, orientation, gauge, completeness, independence, or partition defect. |

The first three rows are mutually exclusive only because zero-fold-support,
one-class multi-arm, and multiple-non-Stokes cases are explicitly routed to
`INCONCLUSIVE`. They are exhaustive only together with that row on the declared
finite domain.

### 11.3 Procedural invalidity

Before execution, absence of any frozen `P0` field stops the design without an
occurrence. In an executed occurrence, violation of `P0` is always `VOID` and
is excluded from the `INCONCLUSIVE` catchall. `VOID` is not a mathematical
fourth mechanism. It is issued for source drift,
post-outcome choice changes, missing seal or run, hidden retry, future leakage,
custody/key/account overlap, undeclared human edits, unequal information or
resources, or evaluator disagreement. A valid but mathematically incomplete
occurrence is `INCONCLUSIVE`, not `VOID`; a protocol violation is `VOID`, not a
scientific null.

## 12. Current populated review

| Review item | Current finding | Disposition |
|---|---|---|
| Same relative-chain type | A parameterized schema is explicit, but no named compactified-relative or rapid-decay chain theory/end model is selected, and the joint action, gauge-fixed space, coefficient line, and full source are not instantiated. | `DEFECT_TYPE_INHABITANCE` |
| `U0/U1/U1'/Uf` cover | `U0` and `Uf` have partial centerlines/local data; `U1`, `U1'`, and every joint overlap map are absent. | `DEFECT_COVER` |
| Source and limit order | A punctured nonzero-lapse scalar order is known; zero-lapse, cap, joint-field, gauge, and fold orders are open. | `DEFECT_SOURCE_LIMIT` |
| Orientation/basis | Local matrices are exact and the root-swap error is excluded; no common Gate-1 orientation line connects the source to fold. | `DEFECT_ORIENTATION` |
| Good ends/divisors | Restricted scalar ends are classified; mixed ends, zero lapse, outgoing box exits, other sheets, and determinant/gauge divisors are incomplete. | `DEFECT_END_CENSUS` |
| Gauge/BFV | Local trace FP admissibility does not license the existing source; a replacement derived source and finite BFV measure are missing. | `DEFECT_GAUGE_BFV` |
| Candidate completeness | No full joint generator, quotient classification, or completeness certificate exists. | `DEFECT_COMPLETENESS` |
| Verdict partition | A zero-fold-support class and a single fixed two-arm combination are uncovered admissible shapes. | `DEFECT_PARTITION` |

The current machine-readable recommendation is therefore

```text
review_authority = SECONDARY_AI_MATHEMATICAL_REVIEW_RECOMMENDATION
review_terminal = DESIGN_REVISE_WITH_NAMED_TYPE_OR_COVER_DEFECT
mathematical_outcome = NOT_EVALUATED
SOURCE_TO_FOLD_LIFT_VERDICT = INCONCLUSIVE
inconclusive_reason = PREEXECUTION_TYPE_COVER_SOURCE_ORIENTATION_GAUGE_COMPLETENESS_AND_PARTITION_DEFECTS
ICE_Gate_1 = OPEN_PARTIAL_PROGRESS
physical_original_cycle = null
global_n_sigma = null
HSWM_G0 = NOT_PASSED
HSWM_G1 = LOCKED
automatic_next = null
```

`SOURCE_TO_FOLD_LIFT_VERDICT = INCONCLUSIVE` here is a design-preflight
classification, not a computed ICE outcome.

## 13. Smallest honest repair order

1. Select one named compactified-relative or locally finite/rapid-decay chain
   theory and end model; then freeze one explicit finite-cutoff holomorphic
   **joint** `m = 2` action, gauge-fixed even integration space, endpoint
   polarization, source normalization, singular set, and chain dimension.
2. Choose one replacement gauge route already left open by the audit—improved
   static with transformed endpoint states and boundary action, or
   time-dependent trace gauge—and derive its finite FP/BFV source. Do not append
   it to the old proper-time source.
3. Prove or reject the full source distribution through `N = 0`, including
   contact terms and one exact cap/`i epsilon`/field/gauge limit order.
4. Construct open `U1` and `U1'` domains, all overlaps, chain-level homotopy
   witnesses, and a common source-to-fold orientation-line transport. Use the
   exact `G^T` cycle coefficient map; never the root permutation `P`.
5. Classify every end, singular divisor, sheet, fold/Stokes crossing, and gauge
   horizon within the declared finite atlas. A box exit remains unresolved.
6. Supply an analytic or exact finite-chain-complex completeness certificate
   and freeze the allowed equivalence/choice family.
7. Either add substantive `NO_FOLD_SUPPORT` and `FIXED_MULTIARM_CLASS`
   mechanisms or preserve the fail-closed `INCONCLUSIVE / PARTITION_DEFECT`
   branch used here.
8. Obtain an independent mathematical owner's signed decision before any OSF
   preregistration, runner, custody ceremony, or HSWM transition protocol.

This order follows the existing ICE result itself: a replacement gauge/source
must precede source-to-complex-cycle and global-intersection work. Installing a
graph backend, signing stack, workflow runtime, or new ontology before these
mathematical fields close remains maintenance.

## 14. Independent mathematical-owner decision form

The reviewer must inspect the exact source bytes and return exactly one terminal:

```text
reviewer_identity =
reviewer_independence_statement =
source_snapshot_digest =
type_fingerprint =
reviewed_defect_uids =
reasoned_findings_digest =

decision in {
  DESIGN_ADMISSIBLE_TO_PREREGISTER,
  DESIGN_REVISE_WITH_NAMED_TYPE_OR_COVER_DEFECT,
  DESIGN_REJECTED_AS_NONDISCRIMINATING
}

signature =
signed_at =
automatic_next = null
```

Acceptance requires affirmative findings for the common type, cover and
overlaps, full source/limit order, common orientation line, relative-good and
divisor definitions, Gate-1 gauge/BFV predicate, candidate completeness, and a
total verdict partition. The reviewer may agree with or reject this AI
recommendation, but may not silently fill an absent mathematical field after an
outcome is known.

## 15. Claim ceiling and stop rule

This packet defines a reviewable contract and reports a pre-execution defect. It
does not define the missing physical original cycle, calculate a source-to-fold
lift, establish a Stokes split, prove a no-go, close ICE Gate 1, pass HSWM G0 or
G1, or show that HSWM invents. No research receipt or results-log entry is due
because no material research outcome was produced.

Until an independent mathematical owner returns
`DESIGN_ADMISSIBLE_TO_PREREGISTER`, stop at design repair. After such a decision,
the next permissible artifact is a frozen prospective protocol—not an immediate
physics run and not an HSWM efficacy claim.
