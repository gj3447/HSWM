# ICE G1 source-type reroute episode v0

> **Status:** `SECONDARY_AI_SCOPED_EXTERNAL_WORKBENCH_RESULT /
> INVENTION_CARRYING_CANDIDATE_TRANSITION /
> DIRECT_V0_TRANSPLANT_RETIRED_IN_EXACT_SCOPE /
> NOT_PREREGISTERED / ICE_GATE1_OPEN_PARTIAL_PROGRESS /
> HSWM_G0_NOT_PASSED / HSWM_G1_LOCKED`
>
> **Date:** 2026-09-04
>
> **ICE blocker:** `open:gate1-original-cycle-signed-global-intersections`
>
> **Selected next ICE discriminator:**
> `open:gate1-starobinsky-finite-m2-replacement-bfv-source`
>
> **Authority:** `SECONDARY_AI_RESEARCH_INTERPRETATION`; the source result is
> owned by the ICE workbench and this document is not independent human
> mathematical approval, HSWM admission, or execution authority.
>
> **Automatic next work:** `null`

## 1. Outcome first

One bounded ICE calculation was executed after its input and runner were
committed. It returned:

```text
KILL_DIRECT_V0_BFV_TO_STAROBINSKY_M2_SOURCE_IDENTIFICATION
```

The exact meaning is narrow: the existing local `V=0`, `U_+`
improved-static BFV zero-mode algebra cannot be reused or relabelled directly
as the source for the frozen Starobinsky finite-`m=2` joint action. The
calculation preserves both objects in their own scopes and does not rule out a
newly derived Starobinsky-compatible source.

This result changes the research order. Construct or falsify one same-model
Starobinsky replacement source **before** attempting `N=0`, `U_1/U'_1`,
the complete end/divisor census, or source-to-fold transport.

It does not close ICE Gate 1. It does not establish HSWM learning, G0, G1,
FCL-1, a physical cycle, a global intersection integer, physics, or a TOE.

## 2. Target identity, evidence, and conceptual delta

| Layer | Bound statement |
|---|---|
| HSWM target identity | HSWM remains one token-native LLM-function macro-neural network whose evolving hypergraph is living harness, world model, and continuous learner under typed outcome-bound dynamics. ICE, this episode, and their graphs are bounded external research interfaces, not HSWM cognition. |
| Prior evidence | The earlier design correctly identified the missing source-to-fold type, but it had no completed relative-chain inhabitant and had not tested whether the strongest available `V=0` BFV candidate shared the Starobinsky source type. |
| New external evidence | A source-pinned symbolic run establishes an exact direct-identification obstruction with 17/17 checks and 3/3 conditional guards. Independent read-only AI review found no verdict-blocking algebra, sign, hash, or scope error. |
| Conceptual delta | The failed direct transplant retires one mechanism family and changes the next discriminator from full atlas transport to a same-model Starobinsky FP/BFV source derivation. The graph serialization merely preserves that scientific delta. |
| HSWM evidence ceiling | There is no HSWM Atom revision, unique owner, Permit, readset/writeset, pre/post state digest, independent future outcome, counterfactual credit, remove/restore intervention, or behavioral effect. G0 remains not passed and G1 remains locked. |

## 3. Source and execution ledger

The source result is retained in the sibling ICE workbench. Its two local
commits are intentionally not described as public pins because ICE was not
pushed without explicit user authorization.

| Object | Immutable locator or digest | Role |
|---|---|---|
| ICE runner/input commit | `e27e0f0017d93c6ff76e7ad10541c4db97a25b8a` | Commits the input manifest and runner before execution. |
| ICE result/report/graph commit | `c8c46f83c43caa3b26499149a8eb35505ad843a1` | Retains raw result, human-readable proof, schema-valid evidence snapshot, scoped claim, evidence, scope, sources, and changed-selection open problem. |
| Input manifest | SHA-256 `c36e4d0a5b173b967d092a6daa05dac3c7a872c00e9eb332fb108f59753d4cfa` | Freezes the bounded question, upstream hashes, comparison fields, false signal and null outputs. |
| Runner | SHA-256 `67eaf2cdf6c6e1d9132c688ac9a65685f51d9a9af3d26df3537782315f144d02` | Performs the exact symbolic reconstruction and source-type audit. |
| Raw result | SHA-256 `38ae973ae85c380913cb038a6c495a7db78f331e9eec38bb2ab5e34f3d7c028e` | Machine-readable result SSOT. |
| Self-excluding canonical JSON payload | SHA-256 `5411e04686348f4f18fa5a3c8261955b317cfa9f4ac24f35ca229ce75285017d` | Internal payload integrity identifier; not a timestamp or custody proof. |
| ICE report | SHA-256 `3c677d11e3e73cfb8d270ad7a3d1239fc075341686070141497fd90773ce0220` | Exact equations, mismatch table, chain-type boundary and review result. |
| ICE evidence snapshot | SHA-256 `659669dc3e1b505b24157a1d9466600f57a722965e86d8c7d8fe1036904619a8` | Schema-valid compact evidence projection; it does not replace the raw result. |

Observed command:

```text
./ice run gate1_m2_v0_bfv_source_type_compatibility
```

Observed output was `VALID_RUN`, with 17/17 exact checks, zero numerical
samples, 3/3 conditional theorem guards, zero automatic descendants, and
`gate1=OPEN_PARTIAL_PROGRESS`.

Local ICE paths are:

```text
/home/lagyeongjun/CD/ICE_ORCA_DRAGON/
  cpt_temporal_folded_susy/
    GATE1_M2_V0_BFV_SOURCE_TYPE_COMPATIBILITY_INPUTS.json
    gate1_m2_v0_bfv_source_type_compatibility.py
    GATE1_M2_V0_BFV_SOURCE_TYPE_COMPATIBILITY_RESULT.json
    GATE1_M2_V0_BFV_SOURCE_TYPE_COMPATIBILITY.md
  ontology/cpt-temporal-folded-susy/
    graph.json
    evidence/gate1-m2-v0-bfv-source-type-compatibility-result.json
```

## 4. Exact mathematical result

For

```math
A=a_b+\frac{x}{2},\qquad
\Phi=\phi_b+\frac{q}{2},\qquad
V(\Phi)=\frac34\left(1-e^{-\sqrt{2/3}\Phi}\right)^2,
```

the frozen bosonic target is

```math
S_2=-\frac{24\pi^2Ax^2}{T_{\mathrm{prop}}}
+\frac{4\pi^2A^3q^2}{T_{\mathrm{prop}}}
+2\pi^2T_{\mathrm{prop}}[-3A+A^3V(\Phi)]
```

on

```math
X_2=\mathbb C_{a_1}\times\mathbb C_{\phi_1}
\times\mathbb C_{T_{\mathrm{prop}}}^{\ast}.
```

The independent momentum pushforward verifies the same action under
`T_prop = iz`. This fixes the exact bosonic `m=2` target, not its
missing full BFV measure.

With `P = a p_a / 2`, the source comparison gives

```math
C_{\mathrm{Star}}-C_{V=0}=2\pi^2a^3V(\phi),
```

```math
\{p_\phi,C_{\mathrm{Star}}\}
=-2\pi^2a^3V'(\phi),
```

```math
F_{\mathrm{Star}}-F_{V=0}=-24\pi^4a^6V(\phi),
```

and

```math
S_{2,\mathrm{Star}}-S_{2,V=0}
=2\pi^2T_{\mathrm{prop}}A^3V(\Phi).
```

The old `p_phi` is therefore nonconserved for nonzero `V'(phi)` and cannot
serve as the `V=0` cyclic invariant without a new Starobinsky canonical
transform. The finite-action correction depends on interior `q` and
`T_prop`, so it cannot be absorbed as an endpoint-only constant.

The remaining typed mismatches are also exact:

- `T_prop`, the `V=0` Darboux clock `T_clock`, and BFV multiplier `N` are three different
  objects;
- fixed old `(a, phi)` endpoints and improved-static `(T_clock, Phi)`
  endpoints have no supplied exact transform;
- the `V=0` result has no finite-`m=2` BFV trajectory lattice, equality or
  deformation to the proper-time source, or absolute determinant/Pfaffian
  orientation.

The positive-real-`phi` guard is conditional on the frozen endpoint or a
positive-real neighborhood. It is not evidence for an unconstructed complex
source corridor. The symbolic bulk mismatch does not require that extra
claim.

## 5. Named chain theory and exact status

The working chain type is now named as the **Pham--Witten relative
singular-chain lane**:

```math
h=\mathrm{Re}(S_2/\hbar),\qquad
X_{\mathrm{good},M}=\{x\in X_2\mid h(x)\ge M\},
```

```math
\mathrm{RC}_{M,3}
=C_3(X_2,X_{\mathrm{good},M};
\mathbb Z\otimes\mathcal L_{G1}).
```

Its exact status is:

```text
SELECTED_TYPE_HYPOTHESES_OPEN
```

The notation fixes a candidate type, degree, regulator role and coefficient
line placeholder. It does **not** assert an inhabitant or production chain.
The following remain null:

- a same-model finite-`m=2` BFV source and typed source deformation;
- the common source/chain/FP-BFV orientation line `L_G1`;
- a compactification or equivalent end model and actual
  `X_good,M`;
- tameness, finite nondegenerate critical points, a regular chamber and
  regulator stability;
- a thimble basis or finite-chain quasi-isomorphism proving completeness.

Hien rapid-decay homology remains an alternative hypotheses-open lane. It is
not selected until a suitable flat/meromorphic connection, controlled
compactification and divisor, real-oriented blow-up, directional decay
condition, coefficient line and comparison map are actually constructed.
The cited frameworks do not choose the ICE cycle or its integer.

## 6. Requested six-stage programme status

| Stage | Outcome at this checkpoint | Next admission test |
|---|---|---|
| 1. Named chain theory/end model and exact joint `m=2` action | Exact bosonic action fixed. Pham--Witten singular-relative chain type selected with hypotheses open. End model not constructed. | Same-model BFV source and one actual coefficient/orientation line, then verify chain hypotheses. |
| 2. Replacement-gauge FP/BFV source | Existing `V=0` direct transplant killed in exact scope. Two alternatives remain live and unconstructed. | Derive or falsify one `STAROBINSKY_CONSTRAINT_ADAPTED_IMPROVED_STATIC_SOURCE` or `STAROBINSKY_TIME_DEPENDENT_TRACE_SOURCE`. |
| 3. Source and limit order including `N=0` | Not admissible yet. | First construct the source; distinguish BFV `N=0` from the excluded action pole `T_prop=0`; then prove distributional extension and noncommuting limit order. |
| 4. `U_1/U'_1`, overlaps and orientation transport | Undefined in a common source type. | Build `X_gf`, all open overlaps/triple cocycles, source-preserving homotopies and `L_G1` transport. |
| 5. End/divisor census and candidate completeness | Prior work supplies only partial candidate end records. | Prove an all-end/divisor/Stokes census plus either a tame Morse/thimble basis or a finite free-chain complex with a proved quasi-isomorphism. |
| 6. Independent review and preregistration | Independent AI read-only math audit passed; independent human mathematical owner and custody review do not exist. | Human owner signs the full source/type/cover/census proof. Only then may a future prospective occurrence be preregistered. |

Consequently `UNIQUE_LATERAL`, `STOKES_SPLIT`, and
`NO_ADMISSIBLE_LIFT` remain unevaluated. Missing inputs are null, not zero, and
do not count as `NO_ADMISSIBLE_LIFT`.

## 7. Outcome-bound invention transition

This episode now contains the previously missing four-part transition:

| Transition field | Bound content |
|---|---|
| Unexpected outcome | The strongest available replacement-source candidate is internally valid in local `V=0` scope but is the wrong type for Starobinsky finite `m=2`. |
| Competing mechanisms | `DIRECT_V0_TRANSPLANT` is `RETIRED_WITHIN_EXACT_CROSS_SCOPE_IDENTIFICATION`; `STAROBINSKY_CONSTRAINT_ADAPTED_IMPROVED_STATIC_SOURCE` and `STAROBINSKY_TIME_DEPENDENT_TRACE_SOURCE` remain `LIVE_UNCONSTRUCTED`. |
| Discriminating question | Can one regular nonzero-potential component derive its own endpoint-improved, constraint-adapted FP/BFV finite-`m=2` trajectory source and a typed comparison to the proper-time kernel? |
| Changed future selection | Perform that same-model derivation/falsification before `N=0`, cover transport, end census, or the final lateral trichotomy. |

This changes the exact mechanism set `M` and next discriminator `Q(y)`.
Under the episode classifier it is an `INVENTION_CARRYING_CANDIDATE` for the
ICE research route. It is not HSWM learning because HSWM did not produce or
admit the outcome through its owner/Permit/outcome-credit recurrence.

## 8. Frozen next discriminator

The next bounded research object is a **source construction or obstruction
packet**, not another end ontology or numerical tail search.

It must bind one of the two live source families and return exactly one of:

```text
STAROBINSKY_SOURCE_CONSTRUCTED_IN_DECLARED_COMPONENT
STAROBINSKY_SOURCE_OBSTRUCTED_IN_DECLARED_COMPONENT
INCONCLUSIVE_SOURCE_CONSTRUCTION
```

Minimum pre-outcome fields are:

```text
model and component
old canonical variables and constraint
new gauge condition and FP bracket
canonical transform or time-dependent generator
endpoint polarization and improved boundary action
BRST/BFV charge and gauge-fixing fermion
finite m=2 field/ghost/multiplier lattice
source measure and integration order
proper-time-kernel equality or explicit deformation
determinant/Pfaffian orientation line
N=0 versus T_prop=0 type separation
domain, horizon, excluded divisor and failure predicates
```

Acceptance requires all exact identities, nilpotency in the declared finite
model, endpoint BRST stability, an invertible typed map to the frozen bosonic
target, and no silent deletion of determinants or boundary states.

The construction is killed in its declared component if a proved algebraic or
global-domain obstruction prevents the required source type. Failure to find a
transform, missing coverage, or an unresolved measure yields `INCONCLUSIVE`,
not a no-go.

No follow-up runner may be executed automatically. A new input manifest and
runner must be reviewed and committed before a separate bounded run.

## 9. Standards-first graph projection

The HSWM standards profile is reused; no new ontology or external graph
backend is introduced. The minimum read-only projection is:

```text
SourceArtifact
  -> usedBy -> ExactAuditActivity
  -> generated -> ScopedObstruction
  -> disposesWithinScope -> MechanismDisposition
  -> motivates -> ChangedSelectionProposal
```

The stable standards lane is RDF 1.1/N-Quads, PROV-O/PROV-DM for asserted
provenance, SHACL 2017 for structural constraints, JSON-LD 1.1 for a pinned
interchange view, and RDFC-1.0 only when a sealed RDF dataset digest is
required. RO-Crate 1.3 is post-terminal packaging. OpenLineage remains optional
run telemetry. Draft SHACL 1.2, RDF 1.2 and SPARQL 1.2 features remain
non-promoting experiments.

For this result, the projection must retain:

- raw/input/runner/report/snapshot paths and SHA-256 digests;
- the exact scope and the proposition contradicted within it;
- both preserved source objects and every explicitly not-killed mechanism;
- the full null-output set and Gate-1 ceiling;
- predecessor evidence, one changed question, and `automatic_next=null`;
- independent-review type, including that AI review is not human custody.

SHACL can require those fields and cardinalities. PROV can express use,
generation, derivation and association. RDFC can produce canonical RDF bytes.
None establishes scientific truth, correct execution, time order, signer
identity, Permit, causal credit, or HSWM Learn.

The ICE graph already records the scoped claim/evidence/scope/open-problem
transition and passed its native ontology, RDF/SHACL, retrieval, competency and
workflow checks. HSWM does not duplicate those nodes into its canonical
ontology; this document is a source-bound research projection.

## 10. Independent review and preregistration gate

The completed read-only AI review independently checked:

- input, runner, three upstream results, raw result and self-payload hashes;
- the constraint and potential difference;
- the sign of the `p_phi`--constraint Poisson bracket;
- the potential-dependent trace FP identity;
- the constraint-root correction;
- the finite-action correction and its non-endpoint dependence;
- all stationary momenta, canonical pushforward and Wick identity; and
- the exact scope of the kill verdict and every fail-closed null.

No verdict-blocking error was found. Two wording restrictions are adopted:

1. old `p_phi` is nonconserved and cannot be used as the `V=0` cyclic
   invariant without a new transform; the coordinates are not claimed to be
   intrinsically unrelated;
2. the positive-`phi` guard applies only to the pinned real endpoint or a
   stated positive-real neighborhood, not an unproved complex corridor.

This is not the independent human mathematical review requested for
preregistration. Preregistration remains prohibited until a human owner reviews
the new same-model source, chain hypotheses, `N=0` order, full cover,
orientation, census and completeness proof and returns an explicit admissible
decision.

The OSF/DSSE/in-toto/Rekor/RFC 3161/WORM/Temporal/drand/custodian/dual-inspector
stack remains a future prospective protocol only. It must not be used to seal
an incomplete mathematical design or to make this retrospective result appear
prospective.

## 11. Artifact classification

The ICE symbolic result is a material bounded negative research result in the
ICE workbench. Its changed selection is the research delta. The ICE graph and
this HSWM document are provenance and design infrastructure around it.

No HSWM content-addressed result receipt or `F1_R8_RESULTS_LOG.md` entry is
created here because HSWM did not execute or admit a material HSWM result.
Creating one would misattribute an external scoped result as HSWM progress.

## 12. Primary sources

- [Witten, *Analytic Continuation Of Chern--Simons Theory*,
  arXiv:1001.2933v4](https://arxiv.org/abs/1001.2933v4) supplies the general
  relative-cycle/Picard--Lefschetz framework, not the ICE cycle.
- [Hien, *Periods for flat algebraic connections*,
  arXiv:0803.3463v1](https://arxiv.org/abs/0803.3463v1) supplies a
  rapid-decay-homology and period-pairing framework under its hypotheses, not
  an ICE compactification or source.
- [Matsubara-Heo, *On the rapid decay homology of F. Pham*,
  arXiv:1705.06052v1](https://arxiv.org/abs/1705.06052v1) supplies a comparison
  framework, not candidate completeness for this action.
- [Henneaux, Teitelboim and Vergara, *Gauge invariance for generally covariant
  systems*](https://arxiv.org/abs/hep-th/9205092) supplies the
  endpoint-improved canonical-gauge boundary, not this source construction.
- [García, Vergara and Urrutia, *BRST-BFV quantization and the Schwinger action
  principle*](https://arxiv.org/abs/hep-th/9511092) supplies BFV endpoint
  framework, not the Starobinsky finite-`m=2` measure.
- [PROV-O](https://www.w3.org/TR/prov-o/),
  [SHACL](https://www.w3.org/TR/shacl/), and
  [RDF Dataset Canonicalization 1.0](https://www.w3.org/TR/rdf-canon/) define
  the stable provenance, validation and canonicalization boundaries used by
  the standards profile.
- [RO-Crate 1.3](https://www.researchobject.org/ro-crate/specification/1.3/index.html)
  remains post-terminal packaging only.
