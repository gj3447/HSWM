# Fresh B0 successor — prospective v2

[Protocol v2](protocol.v2.json), SHA-256
`4ee3e083e59317e443f40fe1f0eea9a8269f60747a5146acb028ae174bfaa68c`,
declares a new no-learning B0 occurrence. It is frozen before its own
selection, environment, model request, or outcome. The consumed 2026-08-30
B0 occurrence remains an inconclusive measurement with no rate and is never
retried or resumed. Its exact protocol, result, and evidence hashes are part
of this successor's start closure.

The actor, model/image/GPU, qualified PDDL-only environment, 8 train + 4
descriptive `valid_seen` allocation, 20-action horizon, 240 tokenize + 240
completion caps, no-retry rule, and complete-run headroom interpretation are
unchanged. The current source includes the separately demonstrated upstream
sandbox import-path repair. The supplied asset root must be the locator's
actual relative root and every selected file must pass its prelease check.
No learning, lesson, revision, retrieval, or few-shot state is introduced.

The successor has a new study UID, version, protocol hash, and public
selection manifest. These inputs produce a fresh deterministic draw, without
replacement within each declared split. Groups or games can overlap earlier
B0/B2 selections by chance; no previously observed task label or outcome is
used to choose the draw. Cross-occurrence exclusion and a paired estimand are
not claimed. `valid_unseen` stays unselected and its record details remain
undecoded.

The old protocol's amendment list is retained as **historical engineering
lineage**, explicitly identified as such in the JSON. The new registration
status refers to this successor, not to all B0 work ever performed. Its public
selection is committed at
`manifests/HSWM_ALFWORLD_B0_SUCCESSOR_SELECTION_2026-09-06.json` before the
live occurrence. Live preflight rejects the old selection-manifest path for
this successor and binds the predecessor result/evidence hashes.

Any comparison with B2 v3 is descriptive and unpaired. B2 has its own selected
groups, external text state, and separately reported reflection costs; an
observed rate difference is not a matched effect estimate or evidence of
efficacy. This study does not pass G0, evaluate G1, establish independent
custody, or admit canonical HSWM learning.

Selection uses `select_prospective_b0` with this exact protocol identity.
The live entrypoint remains `python -m hswm.experiments.alfworld_b0_live`,
through `hswm-run`, with the new private selection, committed public manifest,
and fresh service/cache/output roots. A material complete, inconclusive, or
void terminal is recorded once. Original B0 public aggregate projection
limitations are handled by a separately identified posthoc verification of
the preserved private receipt, never by inventing a missing denominator.
