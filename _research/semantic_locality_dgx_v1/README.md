# Bounded DGX local semantic probe

The 2026-09-20 run is complete; do not overwrite its durable run ID. See the [result and limitations](../../results/HSWM_LOCAL_SEMANTIC_PROBE_2026-09-20.md).

`run.mts --protocol` prints the frozen 80-request design without model calls. A live run requires DGX `hswm-run`, an existing local service with the declared model alias, the repository's pinned Effect dependency, and a new occurrence ID. This study did not independently attest the loaded checkpoint. Pin that before any confirmatory successor.

The script records every response digest and synthetic observation without retries. It does not execute the HSWM canonical semantic runtime, learn a relation, publish KG records or prove efficacy. `restored` repeats the original prompt. `analysis.v1.json` clarifies the changed-input role-swap diagnostic without rewriting the original metrics.
