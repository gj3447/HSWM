# Native command migration checkpoint — 2026-09-13

HSWM-owned command implementations now include native TypeScript/Effect occurrence preparation and readback (all 12 commands), legacy replay, Markdown math compilation, selected efficacy verification, research-fabric management, and production hypergraph SHACL validation. The full migration remains incomplete; see [the successor inventory](../../_research/native_migration_2026-09-13/inventory.v2.json).

The conceptual delta is implementation language, immutable domain functions and typed Effect I/O. The constitution, composable fractal HSWM target, FCL-1..8 contracts, success thresholds, RED paths and outcome-bound learning requirement are unchanged. These tools and this KG are bounded development interfaces.

## Adversarial result and repair

The earlier core checkpoint at 5870f1e passed its tests but disagreed with the original USL implementation on **308 of 3,191** adversarial cases: 293 false accepts, 13 unexpected rejections and 2 output differences. Its original record is preserved. The new [failure receipt](../../_research/native_migration_2026-09-13/usl-differential-failed-attempt.v1.json) binds the failed implementation and corpus.

The repair validates complete v1/v2 plans, policy and report schemas, names, roles, contracts, resolution pins, selected-link declaration order, noncanonical object key order, and Python-compatible calendar/time semantics. Exact original-source fixtures now agree on 33 initial, 3,191 differential and 44 coherently re-signed USL cases, plus 384 datetime cases. These counts are internal oracle comparisons, not the Vitest test count or proof of universal equivalence. Regular tests use static pinned fixtures without Python.

## Verification

An isolated checkout containing only the 61 pinned command files passed **1,073 tests across 161 files**. Ten existing opt-in external integration cases remain skipped. Strict TypeScript checks, Effect boundary and functional lint, both builds and npm packaging dry-run passed. Four freshly emitted CLIs ran from a foreign working directory with only Node on PATH; the hypergraph rehearsal exercised native SHACL. Exact test scope, hashes and retained failed setup attempts are in [the verification record](../../_research/native_migration_2026-09-13/command-verification.v1.json).

Native efficacy output matches the original selected v4 snapshot byte for byte. Legacy replay checks all 296 original manifests and tests materialization in isolated Git repositories. Research-fabric tests cover private state, process identity, time bounds and detachment; no live service deployment is claimed. Occurrence CLI preparation and readback are separate from unverified external audit and sealed completion paths.

The local hswm-dev plan/run/status/feedback cycle passed its 53 runtime route tests. Its feedback source is agent(codex), explicitly scoped to development-route usefulness. Private runtime state and raw logs are excluded from the public KG.

## Remaining work

Continue full occurrence integrity, dual evaluator, audit and completion/publication parity; complete concrete graph publisher transactions and graph-standards fetch/qualification. Then replace active developer-profile, CI and systemd callers without deleting their criteria. P1 retrieval diagnostics, active numeric execution, formal verifiers and generic ontology maintenance require complete native implementations before their Python paths can be retired. Compatibility and hash-bound historical bytes stay preserved.

No new scientific result, causal credit or HSWM efficacy follows from this engineering checkpoint.
