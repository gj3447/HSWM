# HSWM native migration inventory — 2026-09-13

Status: `SOURCE_BOUND_OPERATIONAL_INVENTORY_NOT_MIGRATION_COMPLETENESS_OR_EFFICACY`

This inventory distinguishes the current TypeScript/Effect default for new runtime work from Python surfaces that remain deliberately active, bounded, or historical. It does not claim that the 842 tracked Python files are retired, nor does a native counterpart establish research progress, causal credit, learning, consciousness, or efficacy. The canonical target remains the token-native, schema-approved atom and typed-relation HSWM described in the Constitution; these launchers and projections are bounded tooling around that target.

The machine-readable record is [_research/native_migration_2026-09-13/inventory.v1.json](../../_research/native_migration_2026-09-13/inventory.v1.json). Every entrypoint or grouped surface names the SHA-256 observed in this working tree, its dependencies, counterpart where one exists, and evidence location. The baseline commit is `974c64418c9f29248eb538ffa78912bfa12eb0ea`; untracked launchers are explicitly marked as working-tree observations rather than attributed to that commit.

| Status | Meaning | Current surfaces |
|---|---|---|
| `VERIFIED_NATIVE` | An observed launcher executes a TypeScript/Effect process. It does not certify the result. | `hswm-live`, `hswm-kg-bundle`, native `hswm-task`, research graph, backup, reluvator check |
| `PARTIAL` | A native counterpart exists, while Python remains in an active command or validator lane. | task, USL, G0 occurrence, hypergraph projection SHACL validator |
| `PYTHON_ACTIVE` | The inventory has active operational or CI evidence and no verified replacement for the full surface. | efficacy verifier, research fabric, graph standards, P1 diagnostic, Python toolchain launcher, CI, F1/F3v2 scripts, maintenance scripts |
| `HISTORICAL_ORACLE` | Exact replay or comparison remains bounded to historical bytes without classifying an active implementation as retired. | S2S Python numeric closure |

The root package declares eight Python scripts: `hswm-p1-gate-diagnostic`, `hswm-verify-efficacy`, `hswm-legacy-replay`, `hswm-research-fabric`, `hswm-graph-standards`, `hswm-g0-occurrence`, `hswm-task`, and `hswm-usl`. The inventory records each separately. `_research/root_compat/pyproject.toml` only exposes the first three as a compatibility package. The graph runtime project is a separate locked Python lane for standards tooling.

`hswm-dev` profiles `game`, `maplelineage`, `supullim`, `reluvator`, and `hswm` select checked-in JSON programs through the native adaptive CLI. `adaptive-cli.ts:21` selects `adaptive_hswm_development.v3.json`; its `usl`, `ontology`, and `docs` cells explicitly run `src/hswm/development/bin/hswm-python ... pytest`. Therefore `hswm-dev hswm` is partial, and its routed docs check is Python execution. This does not remove the independently active Python toolchain launcher, bounded SHACL subprocess, or S2S oracle closure.

GitHub Actions retains Python/uv lanes for the broad test suite, wheel-install compatibility, graph-standard requalification, Neo4j publication integrity, SWM-0R replay, SWM-0W confirmatory work, and S2S pilot work. The Effect-runtime job is native for its own build and tests, while it intentionally invokes some Python evidence checks. Those CI facts establish invocation only.

The catalog intentionally covers remaining efficacy, research-fabric, legacy replay, graph-standards, P1, math/formal, compiler, and publishing lanes. Legacy replay actively materializes and verifies migrations; formal verifiers and publication compilers are active implementations until a complete native replacement is evidenced. Historical inputs must be preserved during any port. The catalog excludes Python tests, fixtures, research experiments, receipts, and compatibility modules unless a maintained command, launcher, subprocess, package entrypoint, documentation command, or CI workflow directly establishes an active role. That exclusion avoids turning file count into a migration or scientific-progress metric.
