# Native operations migration checkpoint — 2026-09-13

Native TypeScript/Effect now covers development profiles, bounded Neo4j projection publication, graph-tooling commands, four formal verifiers, repository ontology validation, occurrence integrity through publication, and frozen-cache P1 diagnostics. Full migration remains incomplete; see [the successor inventory](../../_research/native_migration_2026-09-13/inventory.v3.json).

The conceptual delta is implementation language and explicit typed effects. The constitution, recursive HSWM target, FCL-1..8 contracts, original success criteria, RED paths and outcome-bound causal learning remain unchanged. Repository tooling and its KG are bounded interfaces. Engineering parity does not demonstrate HSWM efficacy.

## Verified scope

The isolated checkout contains 100 exact pinned changed files. The full suite passed 1,249 tests but one setup suite could not find its locked drand dependency cache. After copying that already installed exact cache, the failed suite passed all 9 tests. Combined validation covers 1258 passing tests in 181 files, with 10 existing opt-in external integration cases skipped. No source bytes, tests, exclusions or criteria changed between these runs. Strict TypeScript, Effect boundary and functional checks, both builds and package dry-run passed. The [verification record](../../_research/native_migration_2026-09-13/operations-verification.v1.json) contains scope, hashes, original-contract counts, command observations and limitations.

The native P1 command reproduces the entire original diagnostic and stdout for zero, positive (+1) and negative (-1) fresh-gate cases. The fixtures caught ZIP64/UTF-32 cache parsing, random-generator, float hash and split-manifest errors. Three deterministic synthetic cases and 72 bootstrap cases are bounded replay evidence. Missing-cache model embedding generation remains unfinished; no new arm result is claimed.

The original successful occurrence audit is explicitly a mocked verifier fixture. Native completion and RO-Crate/OpenLineage publication match its complete output, and tests reject copied records, changed material, wrong roles and inconsistent chains. Real G0 toolchain qualification remains BLOCKED. Descriptor-backed execution prevents pathname replacement between verification and use; this does not assert that mutable file contents are globally immutable.

The v4 development profile replaces the three active Python check routes with native TS checks. Migration from the exact v3 profile preserves local learning events, refuses a live runtime lock and rolls back atomically on failure. Publisher tests cover whole transaction rollback, readback/count mismatch and a late query after timeout. Formal checks use an observed local OpenSSL executable for the canonical Ed25519 vector; it was not downloaded or represented as a pinned vendor artifact.

Systemd source now invokes the native process, and active Effect CI invokes the native efficacy verifier. No deployment, service restart, external graph write or external publication was performed. Existing Python packaging compatibility and checked-in historical evidence remain.

## Inventory correction and remaining work

The prior inventory incorrectly called both fixed knowledge-map/research-tooling compilers missing. They already existed in commit 5870f1e as pure domain compilers plus typed Effect catalog I/O and native command routes. The successor records this correction while preserving the earlier inventory.

Active S2S numerical execution, uncached P1 embeddings, infrastructure smoke clients, F-series operational callers, official graph-suite successor integration and Python package entrypoints still require work. Each must keep its source-bound refusal and outcome criteria. No amount of passing engineering tests substitutes for a controlled causal-learning result.
