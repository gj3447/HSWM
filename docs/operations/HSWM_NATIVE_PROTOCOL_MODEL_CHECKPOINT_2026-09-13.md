# Native protocol and local model checkpoint — 2026-09-13

P1 now runs the pinned local model when its embedding cache is absent. F1 registry, structural manifest and prompt payloads are native. S2S complete test scores reproduce original receipt bytes under a specified numerical profile. Full migration is still incomplete; see [inventory v6](../../_research/native_migration_2026-09-13/inventory.v6.json) and [active route audit](../../_research/native_migration_2026-09-13/active-route-audit.v6.json).

The conceptual delta is native execution and explicit source/backend binding. HSWM remains one token-native evolving hypergraph macro-network. Target identity, FCL-1..8, RED paths and outcome-bound success criteria are unchanged; ontology and tests do not establish cognition or efficacy.

The [verification record](../../_research/native_migration_2026-09-13/protocol-model-verification.v1.json) binds the 31 final source files, a full isolated run (1365 passed, 10 existing opt-in skips), and the subsequent P1 provenance/cleanup recheck. These are combined checks, not a single test run over one source snapshot. No checking policy or allowlist was weakened.

S2S includes an original nonzero fit case. The 16-thread OpenBLAS SkylakeX reduction required exact FMA arithmetic, lane folding, thread partition and merge order; scalar and single-thread counterexamples remain recorded. Both entire score receipts now match. The score API takes caller-supplied state/source hashes and does not admit a learned model.

P1 uses [Transformers.js 4.2.0](https://github.com/huggingface/transformers.js/releases/tag/4.2.0), the existing pinned MiniLM weights exported locally, and six fixed artifact hashes. A private captured-byte snapshot survives source mutation and is removed after model disposal or failure. Calls use batches of 64 with bounded tokenization. The observed full-window SEP truncation difference is corrected; source vectors differ by at most 2.6996713131666183e-7 in the four-row qualification. These are measured backend differences, not historical byte identity.

The emitted CLI processed 390 synthetic documents and 390 questions with Python and uv absent from PATH. Cached diagnostics preserve v1 bytes. Cold inference emits v2 with exact backend, input and ordered-vector commitments. [Production qualification](../../_research/native_migration_2026-09-13/p1-native-production-qualification.v1.json) records the complete scope; no private model bytes or dataset contents are published.

F1 strict integer lexemes, Unicode whitespace and getter/sparse boundaries were checked against original inputs. Canonical key lists repaired a NUL collision in candidate tables. Structural manifest normalization does not include tokenizer identity or BPE envelope feasibility. Registry source-accepted falsey non-object overrides remain an explicitly stricter native JSON-object boundary.

Next: complete S2S optimization/model/integrity/task receipts and active pilot/live closure; F1 token meter, exact envelope, physical calls and durable spool; F3v2 runtime and individually classified package/CI routes. No new material scientific result, live publication or causal-learning claim is made. Agent usefulness feedback remains distinct from user feedback.
