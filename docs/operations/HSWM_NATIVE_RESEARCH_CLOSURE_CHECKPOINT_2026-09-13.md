# Native research closure checkpoint — 2026-09-13

The F1 watchdog now runs the TypeScript/Effect offline judge. S2S now has native family generation, archives, complete finite datasets, source-bound seeded initialization and full-task Adam fitting. Full migration remains incomplete; [inventory v5](../../_research/native_migration_2026-09-13/inventory.v5.json) and the [active route audit](../../_research/native_migration_2026-09-13/active-route-audit.v5.json) show the remaining execution paths.

The conceptual delta is a native implementation of additional research dependencies and stronger input/evidence binding. HSWM remains one token-native evolving hypergraph macro-network. The FCL-1..8 contracts, target, RED paths, success criteria and outcome-bound causal learning requirements are unchanged. KG projections and passing checks are engineering evidence, not cognition or HSWM efficacy.

## Verified scope

The isolated 68-file implementation snapshot has combined verification of 1344 tests across 205 files, with 10 existing opt-in cases skipped. The full run passed 1,335 tests but refused the drand suite because its separately locked dependency tree was absent from the isolated copy. Copying that existing tree restored all 9 cases without source or criterion changes; this is a full run plus the failed-suite recheck. TypeScript, Effect boundaries, functional lint, both builds and package dry-run passed. The [verification record](../../_research/native_migration_2026-09-13/closure-verification.v1.json) binds source bytes, checks, original fixture contracts and preserved failures. No functional or boundary allowlist was expanded.

F1 validates call/run/suite receipts, token envelopes/parity and separate gold judgments. Synthetic positive, negative, development, Unicode, large-integer and model-identity collision suites match full original canonical judgments. Generated Unicode tables preserve Python 3.12 semantics independently of the Node Unicode version. The emitted command is tested without Python on PATH; publication refuses preexisting outputs and preserves a preexisting partial file. A bounded suite assembler accepts externally supplied runs, snapshots inputs and rejects duplicate item-arm records. This stricter assembly boundary is distinct from the source-compatible judge, which preserves the original duplicate-map and shallow envelope-projection weaknesses.

S2S checks the full 15,625-world task domain and exact 6,250/3,125/6,250 split commitments. Initialization matches 57 raw tensor hashes across all three arms and boundary seeds. Four full original task fits cover three updates: the default learning rate for each arm, and a smaller-rate T16 run that selects update 2. The default runs selecting update 0 remain visible as failed learning traces; migration parity does not convert them into research success. Full history and final arrays retain the original 5e-14 engineering tolerance. Dataset and initial parameter hashes are exact; original post-update hashes are retained without claiming universal NumPy interchangeability. Native task fitting explicitly issues no protocol-admission receipt.

The complete task exposed a T16 update-2 loss error of 0.27362306299351147 that tiny fixtures had missed. Matching source multiplication/grouping reduced it, then a pinned two-lane non-FMA NumPy einsum sum order removed the remaining failure. Source-derived 16/18-length cancellation, tail and repeated-dot fixtures test this fixed reference kernel. The legal notices and source hashes accompany the adaptation. The original failure is retained; tolerance and success criteria were not loosened.

## Next execution boundaries

Complete S2S score/integrity/task receipts, interventions, bootstrap and pilot/candidate closure before replacing the active live Python oracle. Complete F1 manifest/registry/prompt construction, exact tokenizer/envelope projection, model transport and durable spool. Export and qualify the existing frozen P1 model for a pinned native inference backend, then close F3v2 execution. Existing historical Python sources, package compatibility and receipt-bound replays remain preserved while each active route is separately replaced.

No new material scientific result or efficacy claim is made. Agent usefulness feedback is recorded locally as agent(codex), distinct from user feedback; no private runtime output is added to public KG.
