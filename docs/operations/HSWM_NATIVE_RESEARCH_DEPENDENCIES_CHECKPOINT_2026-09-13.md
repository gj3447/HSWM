# Native research dependencies checkpoint — 2026-09-13

Native TypeScript/Effect now includes both infrastructure smoke clients, source-bound official syntax-suite execution, S2S numerical training dependencies, and PROM9 typed ports. Full migration remains incomplete; [inventory v4](../../_research/native_migration_2026-09-13/inventory.v4.json) identifies active Python dependencies.

The conceptual delta is a native implementation and stricter evidence binding at external boundaries. The HSWM target, FCL-1..8, success thresholds, RED paths and outcome-bound learning contracts remain unchanged. This KG and these tools are bounded interfaces; engineering tests do not demonstrate HSWM efficacy.

## Verification

The final isolated 54-file checkout has combined verification of 1307 tests across 193 files, with 10 existing opt-in tests skipped. The full run passed 1,304 tests and rejected 3 legacy replay materializations because the isolated shared Git clone retained object alternates. After making only its object store standalone, all 6 cases in that failed suite passed unchanged; aggregate counts do not double count the earlier passing cases. Strict TypeScript, Effect/functional checks, both builds and package dry-run passed. The initial isolated run failed during test collection because copied relative executable links pointed to the original checkout and loaded two Vitest instances; the complete failure and corrected full rerun are retained in the [verification record](../../_research/native_migration_2026-09-13/extension-verification.v1.json).

The public commands are `hswm-infrastructure-smoke fabric|phoenix`, `hswm-rdf11-nquads-suite` and `hswm-sparql11-syntax-suite` under `src/hswm/effect-runtime/bin/`. Existing `hswm-python infra` routes now delegate to the native client without invoking uv or Python. Tests exercise the actual MCP SDK over isolated stdio and real OpenTelemetry span creation with mocked transport; no live Phoenix SQL or Temporal result is claimed. MCP client 2.0.0 is an official SDK; N3 and Traqula remain independent implementations qualified against official W3C fixtures. Versions, source commits, package integrity, licenses and the complete lockfile are recorded.

RDF 1.1 N-Quads passed 85/85 approved syntax cases and SPARQL 1.1 passed 94/94 syntax-query cases. A first SPARQL experiment incorrectly included query planning/execution and failed 37 cases; it is preserved as an invalid method, followed by parser-only qualification without exclusions. Each actual manifest/action byte sequence is checked against a frozen Git-derived hash before parsing. These are syntax profiles, not universal conformance. The native CI job runs these successors while retaining historical acceptance-locked replay.

S2S now has pure finite-world scoring, all three forward operators, analytic gradients, independent loss evaluation, clipping and full-batch Adam for caller-supplied arrays and initial parameters. All 15,625 finite worlds are checked; the original fit loop supplies full final arrays and histories for three updates in each arm. Numerical comparisons use a declared 5e-14 engineering tolerance; gradient tensor hashes compare 12-decimal normalized values. Post-update original hashes remain in fixtures and are not asserted equal across runtimes. The seed is preserved as supplied initialization metadata; this API does not generate parameters or validate a task receipt.

Adversarial checks repaired finite-loss rejection caused by gradient overflow, weighted-loss normalization before an overflowing sum, malformed/extra parameter acceptance, ignored budget field order, signed-zero normalization, Unicode whitespace and filler codepoint counting. PROM9 retains exact normalized output/digest comparisons for 37 accepted original cases and rejects 306 original cases; all three structured-output schemas and hashes match. The F1 judge is still unported.

## Completion path

Complete the S2S family/task/dataset/protocol and provenance receipt closure, then qualify an explicit native numeric backend before changing the active live route. Preserve pinned historical NumPy replay. Complete F1 run/call receipts and token envelopes before switching its watchdog, then migrate F3v2 model calls and uncached P1 embeddings. Python compatibility launchers and old receipt-bound sources remain visible in the inventory. No passing test, negative mechanism result, or downstream scale result promotes HSWM efficacy.
