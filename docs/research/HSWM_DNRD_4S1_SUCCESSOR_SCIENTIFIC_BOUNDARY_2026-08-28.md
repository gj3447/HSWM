# HSWM-DNRD-4S1 successor scientific boundary

- Date: 2026-08-28
- Status: `SUCCESSOR_DESIGN / UNREGISTERED / UNMEASURED / NO_SCIENTIFIC_RESULT`
- Experiment ID: `HSWM-DNRD-4S1`
- Maximum result class: `ENGINEERING_CONFORMANCE_EVIDENCE_NOT_SCIENTIFIC_EFFECT_EVIDENCE`

## Canonical role and current evidence

HSWM's target remains one token-native LLM-function macro-neural network:
its evolving hypergraph is jointly its living harness, world model, and
continuous learner.  The target loop is sealed trajectory -> independently
attributable outcome -> outcome-bound, owner-valid canonical revision ->
changed later token-native traversal or transition.  A repository ontology,
CI run, runtime closure, bridge, ledger, marker, or judge is only a bounded
projection or evidence interface; none is HSWM cognition or learning.

DNRD-4 was a deliberately narrow deterministic durable-routing diagnostic,
not that target loop.  Its strongest possible GO was
`DIAGNOSTIC_INTEGRITY_GO_NO_UTILITY_CLAIM`, namely finite engineering
conformance of a response-independent integer routing payload across recovery,
W0 rollback, binding derangement, and fixed-rule replay.  It could never show
LLM learning, HSWM continuous learning, causal credit, utility, independent
sampling, unseen-context generalization, canonical admission, topology change,
or a population effect.

The DNRD-4 candidate did not reach a semantic occurrence.  Before its marker,
the static configuration instrument rejected an invalid manifest ordering:
the producer sorted filesystem `Path` objects component-wise before serializing
them, while the executor and judge require lexical order over the serialized
POSIX path strings.  No Quicknet round was obtained; no semantic marker, model
call, occurrence directory, or judge invocation exists.  This is an instrument-
precondition failure, not a data point or a negative/positive finding about
routing, persistence, recovery, an LLM, or HSWM.

The preserved predecessor pins are Source A
`276fc42354169cb5f0f0bc6cbaf34052047cd630`, preregistration B
`b1dc53d8efdaee24d1ffad10cc558a48321bc6ac`, preregistration SHA-256
`87cdf810e3c4c88a8b755f5b31bd3b98dad6bff9d5c320e58eaeb7b2659a3762`,
and rejected runtime-manifest SHA-256
`fbca6ec3d59fc575f7a9effc4f7add15da8d56e280b5434981f84355f9cdd737`.
Its B CI run `33157246212` succeeded; that engineering fact does not cure the
incompatible producer/consumer canonical-order contract.

## Conceptual delta and new identity

DNRD-4S1 changes only the pre-marker static instrument so that the frozen
manifest ordering is valid.  It preserves DNRD-4's finite diagnostic question,
arms, thresholds, response-independent scorer, and nonclaims.  The correction
defines canonical order over serialized POSIX path strings and makes the
offline registration builder reject a noncanonical runtime closure before B
can be written.  It does not reinterpret, consume, or promote the failed pre-
marker state into evidence.

`HSWM-DNRD-4S1` is therefore a distinct, future occurrence with a new seed
domain and identity.  It requires a fresh Source A, fresh source manifest and
runtime closure, fresh successful Source-A CI receipt, a direct-child Source B
that changes only its own preregistration, and a fresh successful Source-B CI
receipt.  It must use new owner-only bridge-state, attempt-registry, and
occurrence roots; no predecessor root, event, response, pulse, marker, seed,
runtime, or result is an input.  There is no retry, repair, or replacement of
a marked occurrence: once this successor's semantic marker exists, it admits
no resume, duplicate, second pulse, or substituted execution.

The sole seed is derived in the new `HSWM-DNRD-4S1` domain from its frozen
Source-A and Source-B identities plus the new Quicknet chain, eligible round,
and verified randomness.  The sole pulse is the first eligible Quicknet round
at or after `max(Source-A-freeze time, Source-B-CI completion time) + 900s`.
Before that pulse, the executor must verify the exact public Source-B head,
fresh roots, matching source/runtime/preregistration/CI identities, and the
absence of a semantic marker.  It writes and fsyncs its marker before any
generation.  A pre-marker static, chronology, identity, remote-head, or
verifier failure refuses execution without creating an occurrence.

## Frozen bounded diagnostic

The question remains: in one trusted finite occurrence, does a
response-independent scorer-outcome-bound integer routing payload persist
through fresh-process recovery and change pre-model route selection relative
to exact W0 rollback and context-binding derangement, while fixed-rule replay
reproduces W1 without model dispatch?

Four future-seeded streams each force all four-context by two-route cells once
(32 training calls).  `FULL`, `NO_MEMORY_ROLLBACK`, and
`BINDING_DERANGED_NUMERIC_PLACEBO` then receive eight heldout probes per stream
per arm (96 calls), for at most 128 generation calls.  `FULL` must be 8/8
positive per stream; W0 4/8; deranged at most 4/8; FULL differs from W0 on
exactly 4/8 and from deranged on at least 4/8, alongside mandatory recovery,
rollback, replay, parity, leakage, chronology, accounting, and evidence
closure checks.  A valid failed rule is `DIAGNOSTIC_NO_GO`; a contradiction is
`VOID_PROTOCOL`; a sealed bounded operational failure is
`INCONCLUSIVE_OCCURRENCE`.

The model remains a format/liveness boundary, not the learner: a valid dummy
candidate can leave the updates and GO rule unchanged.  The streams are not
independent samples, and the scorer's claimed response-independence is not
established causal isolation.  Thus even a complete GO is only a narrow
engineering result and cannot be elevated to scientific evidence for HSWM.

## Reservation

DNRD-5 remains reserved for a later, separately frozen causal
outcome-bound-learning design: sealed model response, independently hidden and
attributable outcome, credit bound before update, and fresh later behavior
tested against reversal, time-shuffle, and exact-W0 controls with a declared
sampling unit, estimand, error rule, and missingness/publication policy.
Adding deterministic routing mechanics alone is not that study.

The target identity and promotion limits follow
`docs/canon/HSWM_CONSTITUTION_2026-08-20.md`; the predecessor chronology and
finite-rule details remain preserved in
`docs/research/HSWM_DNRD_4_SUCCESSOR_SCIENTIFIC_BOUNDARY_2026-08-28.md`.
