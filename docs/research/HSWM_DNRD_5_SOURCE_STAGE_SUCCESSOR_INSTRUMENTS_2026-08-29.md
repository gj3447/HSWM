# HSWM-DNRD-5 source-stage successor instruments

- Date: 2026-08-29
- Scope: post-Q0 source-stage implementation and adversarial review
- Decision: `SOURCE_STAGE_PROGRESS / NOT_READY_FOR_SOURCE_A / NO_MODEL_CALLS`
- Scientific result: `NO_CAUSAL_EFFECT_RESULT / NO_HSWM_LEARNING_RESULT`

## Canonical role and evidence boundary

HSWM's target identity remains one token-native LLM-function macro-neural
network whose evolving canonical hypergraph jointly plays the roles of living
harness, world model, and continuous learner. DNRD-5 asks a bounded causal
question inside that identity: whether outcome-bound model-proposed revision,
under current typed authority and provenance, changes fresh hidden-probe
behavior relative to three controls.

The work recorded here does not answer that question. It adds source-stage
evidence instruments. Repository code, tests, ledgers, fixtures, and knowledge
graph projections remain bounded interfaces or measurements; they are not HSWM
cognition or learning. The fractal research status remains
`SCIENTIFICALLY_CONNECTED / INTEGRATED_CLAIM_UNJUDGED`, and all eight FCL laws,
including same-rule HSWM-of-HSWMs composition, remain target constraints rather
than demonstrated outcomes.

## Conceptual delta

Q0 is immutable and remains `ONE_CALL_CONSUMED_NONCLOSEABLE`. Its unused call
slots, seed, root, and evidence cannot be resumed, repaired, or relabeled. The
successor work therefore uses new namespaces and addresses three narrower
gaps:

1. A provider call must cross a root-bound, single-use, exact nine-slot
   capability boundary. The provider and capability ledgers must fail closed
   and correlate the same call evidence.
2. The checked-in one-block actual-byte fixture must be consumable as the
   actual typed chronology `W0 -> four fork incidences -> three ADMIT effects ->
   exact RESTORE`, without inventing a mutable fork-state store or claiming a
   provider occurrence.
3. A future Q1 must treat a provider envelope as ordinary strict JSON, compare
   exact assistant-content UTF-8 independently from its canonicalized
   structured diagnostic, materialize every request before the first START,
   and support an independently reconstructed complete closure.

These changes instrument exactness and chronology. They do not establish that
the model learned, that any arm has a causal effect, or that the provider is
globally deterministic.

## Source-stage instruments

### Provider dispatch boundary candidate

The provider gateway's public execution path is being constrained to refuse
before its durable provider START and before network dispatch unless it
receives the exact capability type bound to the same local evidence-root
genesis, block, ordered `1 + 4 + 4` call schedule, and exact call commitments.
Immutable local root identity, hash-chained block and call records, no-resume
behavior, and provider/capability ledger joins are source-stage controls for
ordinary checked-source bypass and ambiguous retry paths.

This remains a source-stage candidate. Capability issuance is not yet an
externally anchored Source-A preflight, future-randomness premarker, canonical
Permit, trusted-time decision, or globally unique occurrence authority.
Its threat model is ordinary public-API use with checked source and a
non-hostile in-process and filesystem boundary. Python import, reflection,
monkeypatch, or evidence-root write access can invoke private paths or rewrite
local ledgers; any such condition invalidates the run and cannot support Source
A. This is execution instrumentation, not a security capability against an
adversarial co-resident process.

### Provider-free one-block projection

The one-block integration consumer first requires the independent actual-byte
fixture judge to pass. It then reconstructs the actual v2 atom envelopes,
typed references, payloads, and journal read sets for:

- one W0 atom and four distinct fork-incidence atoms that all reference it;
- ACTIVE and OUTCOME_INDEPENDENT_SHAM admitted successors whose behavior
  projections reference their admitted macro dispositions;
- a third admitted successor staged for EXACT_W0_ROLLBACK, with no fixture
  behavior projection from that transient staging state;
- the restore transaction whose read set includes W0, the staged successor,
  and rollback decision; and
- DELAYED_NO_CREDIT behavior sourcing W0 and rollback-arm behavior sourcing
  the restore transaction that references W0.

The fixture has no separate mutable production fork-state store. The consumer
therefore establishes typed source topology only, not state equality, provider
behavior, rollback efficacy, or an occurrence.

### Q1 source-stage exactness instrument

Q1 is a new source-stage namespace modeling a future qualification-only
contract. A future frozen Q1 must bind 24 public synthetic cases across all
three call classes, four replicas per case, a seeded frozen 96-slot
permutation, source and first-success CI evidence, an independently built
verifier, a corpus manifest, and a fresh evidence-root genesis. The current
source-stage plan validator binds declared digests and structure only. It does
not yet resolve those digests to independently checked Git, CI, or build
artifacts.

The source-stage gateway has no network callback. It accepts exactly 96
pre-supplied `HttpObservation` byte fixtures and replays them offline through
the future ledger and closure grammar. Its replay terminals are explicitly not
provider observations or provider reproducibility results. A real Q1 runner
requires a separate contract after the external evidence and launcher gate
exists; it is not implemented by this source-stage class.

Before the first Q1 START, all raw instruction, model-input, response-schema,
RNG, and constructed-request blobs must be content addressed and durably
materialized. Raw response envelopes are preserved as opaque bytes. Both the
envelope and assistant content are parsed with an ordinary strict JSON parser
that rejects duplicate keys and non-finite values. Exact assistant-content
UTF-8 identity drives the reproducibility decision; a canonicalized parsed
value is diagnostic and cannot rescue a byte mismatch.

The independent replay closure reconstructs requests from raw material and
validates the model, response cardinality, terminal reason, usage arithmetic,
content schema, identity binding, ledger chronology, and all 96 accounted
terminals. Its conclusions are explicitly replay-reproduced,
replay-falsified, inconclusive, or void for a protocol, ledger, hash, ordering,
or receipt breach. The current observed-isolation input is caller-supplied
canonical receipt bytes, not an independent observation or attestation. It
therefore cannot authorize a real Q1 run until a separate launcher acquires
and binds the required finite process, node, GPU, cache, and runtime
observations.

Q1 source code and tests are not a Q1 preregistration. A Q1 pass, if later
obtained, would provide finite support only for the declared response-exactness
property. It cannot freeze Source A or authorize the 300-block causal study.

## DGX finding and execution rule

The checked-in [Q0 forensic result](../../results/HSWM_DNRD5_Q0_QUALIFICATION_RESULTS_2026-08-29.md)
records the 2026-08-29 execution identity as prefix caching enabled,
`max_num_seqs=6`, another vLLM process observed, and cross-process provider
state not closed. That historical observation does not form the required
dedicated boundary. No stronger claim about the DGX's current process inventory
is made here without a new bound inspection receipt.

Accordingly, that Q0 identity cannot qualify Q1 or a DNRD-5 occurrence. A
future Q1 requires either a separate physical node/GPU or an explicitly
authorized exclusive DGX window with existing workloads quiesced, a fresh
loopback-only provider, one sequence, prefix caching disabled, dedicated cache
and process identity, and before/after receipts. Those controls can support a
finite declared boundary; they cannot prove universal absence of interference.

## Remaining order

1. Pass source-stage adversarial tests and full repository regression.
2. Commit and push the exact source, then require its first successful CI.
3. Reproduce the same commit on DGX through `hswm-run` without model calls.
4. Only in a genuinely isolated provider window, freeze and publish a new Q1
   plan and marker before any Q1 START.
5. Close Q1 independently. Then perform a separate no-call Source-A audit over
   the complete production runner, evaluator custody, Permit/currentness,
   randomness, provider, occurrence judge, and LCB qualification.
6. Only a qualified Source A may proceed to Source B, future beacon,
   premarker, and one immutable 300-block occurrence.

Until those gates pass, the DNRD-5 causal effect is `NOT_EVALUATED`, not zero,
null, positive, or negative.
