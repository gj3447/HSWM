# HSWM DNRD Source-A scientific boundary

- Date: 2026-08-27
- Status: `IMPLEMENTED_INSTRUMENT / UNREGISTERED / UNMEASURED / NO_SCIENTIFIC_RESULT`
- Experiment ID: `HSWM-DNRD-1`
- Family: `REPEATED_CONTEXT_TABULAR_ROUTING_MECHANICS_V1`

## Canonical role

HSWM's target identity remains the one token-native LLM-function macro-neural
network in `docs/canon/HSWM_CONSTITUTION_2026-08-20.md`. Its evolving
hypergraph is simultaneously the living harness, world model, and continuous
learner. This diagnostic is only a bounded projection of one possible durable
routing seam. It is not HSWM cognition, learning, or an efficacy result.

The implementation uses schema-approved local experimental atoms with exactly
one schema-relative routing responsibility owner, typed content/state/journal
references, provenance-bound transitions, and scorer-outcome-bound credit
records. Those structures make a transition auditable. Their existence or a
test pass does not show that the transition is useful or learned by an LLM.

## Current evidence and conceptual delta

The prior P1 scalar path was scientifically RED: twelve candidates produced no
active update or behavioral change. P1v3/P1v4 established only a narrow
synthetic L0 typed-policy actuation precondition. The proposed P1v3v4 L1 causal
lesson was killed before registration and must not be revived.

The present delta is therefore deliberately smaller than an HSWM learning
claim. It asks whether a frozen, outcome-bound integer routing payload can be
written through canonical-atom V2 transitions, survive a clean-process
recovery, change a pre-model tabular route relative to exact rollback, and
lose that alignment when context bindings are deranged. It does not ask
whether the LLM discovers the routing rule, generalizes it, or improves utility.

## Exact construction

- Four future-seeded streams each contain four context keys and two routes.
- Training forces all eight context-by-route cells once per stream.
- Heldout evaluation reuses the same four contexts; it is not new-context
  generalization.
- `FULL` applies locally declared scorer outcomes to the durable numeric field.
- `NO_MEMORY_ROLLBACK` recovers exact `W0`.
- `RAW_EQUAL_BUDGET` deterministically replays the same eight sealed scorer
  records through the same fixed integer rule from `W0`. It is a replay
  fidelity control, not a model-visible raw-transcript arm and not an
  equal-token comparison.
- `BINDING_DERANGED_NUMERIC_PLACEBO` preserves the matched numeric payload
  byte count, precision, update multiset, and norms while permuting context
  bindings within stratum. It does not claim full history, atom, or reference
  parity.
- The maximum is 32 training plus 128 evaluation client-dispatched generation
  requests. Provider-side physical computation and cache behavior are not
  observable.

The model receives preselected evidence and returns the selected evidence
token. Consequently, positive routing separation is substantially
construction-driven. The model is a controlled response boundary here, not
the learner whose generalization is under test.

## Evidence-integrity boundary

The source candidate requires a clean Source-A commit, successful Source-A CI,
an exact source manifest, and a one-file direct-child preregistration commit B.
Every frozen source member must be proved as a Source-A Git blob. The selected
TypeScript runtime and recursive package closure are copied into the occurrence
bundle; the bridge and scorer execute from owner-read/execute-only copied
closures, and each subprocess entrypoint is rehashed before invocation.

The public-pulse verifier command is derived exactly from the pinned Node
executable and DNRD helper and uses the helper's literal
`online --expected-round N` ABI. It runs with a fixed working directory,
secret-free subprocess environment, and timeout. Source A fixes the exact
Linux x86-64 Node 24.13.0 and CPython 3.12.13 executable hashes, Python and
Unicode versions, and the official npm-installed `drand-client` 1.4.2 ESM
bundle SHA-256. B may only repeat those identities; it cannot select arbitrary
self-consistent runtimes.

The scorer uses an exact `-I -S -c` launch contract that disables Python's
global `site`, `.pth`, and `sitecustomize` startup path, then inserts only the
owner-read-only copied source-closure root before running the frozen scorer
module. The Python standard library, dynamic loader, OS, and same-UID host still
remain within the declared trusted-runtime boundary.

The helper checks the exact bundle hash and a bounded lexical policy forbidding
ordinary static, export-from, and dynamic ESM dependencies before loading that
file by exact URL. The occurrence retains those bundle bytes as an owner-read-
only indexed artifact, and the independent adjudicator rehashes them against
the Source-A constant, B, config, runtime receipt, and pulse provenance. This
prevents package-export redirection and ordinary ESM dependency substitution;
it is not a universal JavaScript non-dependency or benign-code proof. The
official round-1000 Quicknet fixture used by CI's offline BLS regression is
also a member of the Source-A closure.

Only the path that internally constructs the frozen production adapters emits
the admissible hash-bound-adapter marker; no caller-supplied dependency path has
a production-admissibility switch. The dependency-injected executor exists for
hermetic tests and marks its output explicitly inadmissible as scientific evidence. DNRD execution is a
repository-checkout-only research surface; the Python source distribution does
not claim to carry or replay its cross-language live verifier closure.

This is not hostile same-UID isolation. The scorer API and runner code path do
not receive, read, or parse private answers, but the scorer process shares the
same OS user and output tree. Its strongest status is
`DECLARED_ROLE_SEPARATION_NOT_PROVEN`. The retained build inputs, compiler, and
selected packages are pinned, but the bundle adjudicator does not independently
re-execute the build. The verifier's preflight file hashes and exact-file load do
not defend a hostile same-UID race between those two operations. The
adjudicator also does not itself re-run BLS verification of the online pulse;
it validates the retained receipt and its exact frozen identities under the
declared source/runtime-trusted boundary. Therefore this is not cryptographic
host attestation and cannot establish provenance against a malicious local
operator capable of fabricating an entire self-consistent evidence directory.

Training provenance canaries are derived from the future pulse and placed only
in training prompts. Candidate emission is forbidden if they appear in any
heldout request, any sealed or retained raw model response, routing key,
durable state evidence, or any raw mount-closure byte. This detects the declared
leakage channel; it is not a proof against every possible side channel.

## Permitted terminal and nonclaims

The strongest possible terminal is
`DIAGNOSTIC_INTEGRITY_GO_NO_UTILITY_CLAIM`. It means only that the frozen
mechanics and evidence-integrity checks were observed in this one occurrence.
It cannot be promoted to an efficacy PASS.

The occurrence may not establish any of the following:

- LLM learning or unseen-context generalization;
- independent outcome generation or scorer isolation;
- utility improvement or durable-state superiority over transcript prompting;
- HSWM continuous learning, topology learning, role specialization, transfer,
  consolidation, or general cognitive uplift;
- exact model-weight identity or provider-cache independence; or
- an independently reproduced TypeScript build.

## Chronological gate

No generation request is authorized by this implementation record. The next
steps are: freeze and push Source A, obtain a successful CI receipt for that
exact commit, materialize the pinned runtime closure, commit exactly one
preregistration file as direct child B, and present B plus the exact
preregistration SHA-256 for explicit external ratification. The first eligible
Quicknet pulse must be at least 900 seconds after both Source A and
ratification. Before exact-hash ratification, the generation-call count remains
zero.
