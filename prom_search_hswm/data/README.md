# PROM source-tree fixture boundary

Only `gold_badiou24.json` belongs to the fresh-clone verification tier. It is a
small compiled structure fixture derived from the repository's Neo4j home-canon
research cycle, not a copy of an upstream source text. Its required SHA-256 is:

```text
7eeac9e6915d645e846014db3e6d1798645f9c7ff90af7d94eb97576e0516489
```

`test_hswm_hypergraph.py` uses the 24 records to verify deterministic incidence,
entity de-duplication, and hub structure. This is a structural regression, not
an efficacy benchmark.

Other files historically placed in this directory—such as
`binding_gold_p1.json`, `real_gold_gfs.json`, and `sources_realfields.json`—are
optional/local research inputs. They remain ignored and are not required by the
documented Tier-1 test command. External benchmark corpora and model caches also
remain outside Git.
