# Open HSWM kernel v2r3 — independent LakatoTree judgment

> **tree**: `LakatosTree_HSWM_SolidMultiAgent_20260722`  
> **node**: `ENG-open-composition-kernel-v2r3`  
> **judged_at**: `2026-07-22T10:54:15.912751Z`  
> **server-derived result**: `partial`  
> **certificate**: `false`

## Result first

The deterministic open-composition kernel is accepted only as a **partial engineering repair**.
The receipt chain is valid and independently rederives `partial`, but certification and scientific
progress are not established.

This judgment does **not** claim retrieval improvement, learned MoE routing, multi-agent transfer,
or B2.1 interference control.

## Frozen evidence

| artifact | SHA-256 |
|---|---|
| neutral judge packet | `bc5b3fb96d073626600e6203f240c644b829192edf24860db9d82cfb487bfb65` |
| injected-negative receipt | `1f9aa36aa3ecbcc1a61ab1de00e956cca608673516bc9f9386c990eed419950d` |
| v2r3 test script | `bb1005916616f61af542fc5c0bfea4f229be5df4b43e0b1265184667d4d7f32c` |
| v2r3 implementation | `d916148a6320c08bce0bd45e272efff9df88aab889f53687745732ba3654ede3` |

The first independent judge correctly refused the implementer-authored evidence packet because it
contained judgment-like fields and no standalone exact negative-injection receipt. No verdict was
submitted in that attempt. The raw history was preserved; a fact-only packet and a separately
hashed negative receipt were then produced for the retry.

## Replay facts

```text
registered target: 59 passed in 0.05s
expanded suite:    78 passed in 0.18s
negative injection: 2 passed in 0.02s
v2r3 script alone: 28 passed in 0.03s
```

The static artifact lineage reports `reproducible=true`, `gaps=[]`, `stale=[]`, with environment
SHA prefix `19ffcec91cb3`.

## Server receipt chain

- prediction receipt: `6fbcf64f46b2037e505027b5928238659ce080cad7f67bb1a01dfb480cd3a278`
- result receipt: `c000bd063ded7d89b4123bb50cc34a7c38ef66a244514e9a555f3edb38e97a60`
- submitted metric value: `1.0`
- novelty: `false`
- Lakatos status: `degenerating`
- measurement grade: `client_asserted`
- receipt verification: `ok=true`, `rederived=partial`, `cache=partial`, `from_receipt=true`

The v2r3 prediction was registered before the green repair measurement but after the two red
counterexamples were already observed. It is therefore disclosed as post-red engineering TDD, not
a blind scientific prediction.

## What the engineering repair establishes

Within the tested deterministic boundary:

1. mount-qualified interface IDs remain injective for the exercised delimiter-heavy cases;
2. public composition closes over atomic and aggregate `OpenHSWM` values without fixed depth;
3. snapshots reject the discovered mutable seam and edge-cluster aliases;
4. isolated vertices, provenance, mount multiplicity, and explicit shared-edge capabilities are
   preserved or fail closed at legacy materialization;
5. binary `canonical_identity` lowers only when exact `left/right` roles remain representable;
6. Python 3.9.6 resolves the exercised public type hints;
7. v1, B0, B2, and adjacent hypergraph fixtures remain green.

These are property-test results over the frozen fixtures, not a general semantic-completeness proof.

## Why certification remains false

LakatoTree reports the missing gates:

- `reproducible`: the certificate node does not recognize a DatasetManifest-style recorded lineage,
  despite the separate static lineage graph being reproducible;
- `calibrated`: `ECE=0.229 > 0.1`;
- `measurement_owned`: the metric is `client_asserted`, not server-regenerated or attested.

The service-generated provenance replay is:

```text
python test_hswm_open_kernel.py JUDGE_PACKET_hswm_open_kernel_v2r3_20260722.json
```

That command is not the registered pytest command and fails with exit `127` because this environment
has `python3` but no `python` alias. Consequently `script_sha_server_verified=false`. The successful
independent command was the exact registered `python3 -m pytest ...` replay. This discrepancy is
recorded rather than papered over.

## Open frontier

- relation-specific type, polarity, role compatibility, and adapters;
- bounded readout over cyclic connector graphs;
- learned agent `CONNECT / SEPARATE / SPECIALIZE` policy and durable event loop;
- B2.1 learned gating and interference control;
- second-benchmark and Agent-A-write to Agent-B-transfer measurement;
- server-owned replay/certification integration using the actual pytest command.

The broader question remains open. The correct current state is **solid deterministic kernel,
partial LakatoTree judgment, no scientific-performance claim**.
