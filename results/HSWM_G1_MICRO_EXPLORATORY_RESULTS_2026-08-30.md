# HSWM G1 micro exploratory result

## Result

The first live minimum G1-shaped occurrence completed on the DGX with the
pre-registered terminal
`EXPLORATORY_OBSERVATION_RECORDED_NO_EFFICACY_INFERENCE`.

The occurrence established that this bounded instrument can execute and close
one local
`trajectory -> outcome -> credit -> permit -> admission -> fresh behavior ->
remove -> restore` traversal. It did **not** produce a behavioral causal
contrast. This is an instrument-validation result, not evidence of HSWM
learning efficacy, a G0 pass, or a G1 gate result.

The checked-in raw bundle has SHA-256
`2a41f3700834cc5b98ddda50203da98d9d82f88fe96e57d61ceb2992f9cd2d50`;
its internal canonical bundle digest is
`58d82e8896362ee6d5d4f7f4de44f6f8adbcea9e365b9322d287444dc756d31e`.

## Frozen occurrence

- Study UID:
  `sym:ExploratoryStudy:hswm-g1-micro-outcome-disposition-2026-08-30`
- Protocol canonical SHA-256:
  `3f608c2167eb839a17812050302e641445be5f9012a09f25980cca578c4449d4`
- Execution source commit:
  `b68010d9d410afcb4f6d219de93bd6f0324fc79a`
- Source tree:
  `d14574c90f94f8af14fdd2a2b6e5a80b40a87bea`
- Source CI: run `33302949060`, all eight jobs successful
- Live wrapper run: `g1-micro-live-b68010d-001`
- Live interval: `2026-08-30T09:11:42Z` to `2026-08-30T09:18:59Z`
- Model: `Qwen/Qwen3.6-35B-A3B-FP8`, revision
  `95a723d08a9490559dae23d0cff1d9466213d989`
- Runtime: vLLM `0.25.1`, NVIDIA GB10
  `GPU-ffed5bca-3452-8e9e-03fb-b2a4d8f40bc5`
- Container image:
  `vllm/vllm-openai@sha256:e4f88a835143cd22aee2397a26ec6bb80b3a4a6fe0c882bcbc63822904766089`
- Network boundary: host ingress was bound to `127.0.0.1:18080`; Docker
  bridge egress was not independently blocked.
- Calls: eight tokenizer preflights and eight completions, all successful,
  with no retry or refill.

The wrapper archive SHA-256 was
`b1b010fd34e9adaafc59b1298b9a28799bec1f04052178875c8221645ff4d94e`.
The durable one-shot registry closed as `COMPLETED_NO_RERUN` and binds the
bundle digest above.

## Five-branch observation

The hidden operator was `ADD_THREE`. The model's sealed training trajectory
selected the correct opaque value for input `7`. ACTIVE feedback was `true`.
The precommitted outcome-independent sham feedback was also `true`, so the protocol
classified that comparison as
`UNINFORMATIVE_SHAM_EQUALS_ACTIVE_FEEDBACK`. ACTIVE and SHAM both proposed and
locally admitted `ADD_THREE`.

| Branch | Compiled rule | Fresh choice | Correct |
|---|---|---:|---:|
| ACTIVE | `ADD_THREE` | `v_0014` | yes |
| OUTCOME_INDEPENDENT_SHAM | `ADD_THREE` | `v_0014` | yes |
| NO_UPDATE | none | `v_0014` | yes |
| REMOVE | none | `v_0014` | yes |
| RESTORE | `ADD_THREE` | `v_0014` | yes |

The pre-registered descriptive five-branch pattern was therefore `false`.
Most importantly, NO_UPDATE and REMOVE were already correct. The prompt exposed
the candidate arithmetic operators, operand, and value-encoded response
choices, so the base model could solve the fresh item without the admitted
state. The task was behaviorally saturated and could not identify mediation by
the local disposition in this measured item and occurrence.

This does not show that HSWM efficacy is zero. It shows that this single
stochastic occurrence did not provide an informative treatment-control
contrast and cannot support an efficacy inference.

## What the instrument did establish

The result and raw journal close the following finite mechanics:

- one trajectory was sealed before outcome feedback;
- the same-process evaluator emitted a provenance-bound outcome;
- ACTIVE and SHAM proposals, credit decisions, local grants, single-use
  consumptions, and admissions joined by content hashes;
- exactly one cue-bound disposition was admitted per updating branch;
- the ACTIVE state moved from genesis
  `c67d1a9f965cfffb0720782940445557d3f1328a414736e76436e7556d83bb2f`
  to
  `0efc167fff6df499d864ae5c39293d2790d61bb8e877af9faf77b0e948aae1ed`;
- REMOVE returned to the exact genesis hash, and RESTORE returned to the exact
  ACTIVE hash;
- the 64-event journal accounts for all eight calls and preserves raw request
  and response preimages;
- the fresh pinned container was removed and the three pre-existing shared
  service container identities were restored.

These are integration and measurement facts. The local grant is explicitly
not an Atom v2 Permit, not repository-canonical HSWM admission, and not HSWM
cognition.

## Separate frozen local structural verification

In a separate wrapper run, the checked-in verifier replayed the original result, protocol, exact durable
registry path, startup and final runtime observations, SQLite logical state,
eight-request metric, teardown, and shared-service restoration. It returned:

- `VALID_LOCAL_PROTOCOL_REGISTRY_BACKEND_BINDING`
- `VALID_LOCAL_DGX_FINAL_ATTESTATION_AND_RESTORATION_JOIN`
- `VALID_LOCAL_FROZEN_DGX_EXPLORATORY_EXECUTION`

The verification wrapper run was `g1-micro-verify-b68010d-001`; its receipt has
SHA-256
`6a83e4312e46eaaea2cae3f6b1a3e50b17a2e3e8f3e0ea0be491907bca028aa8`.
This was local structural verification on the same source commit, not an
independently owned scientific adjudication or independent G0/CF-07 evidence.

The checked-in registry seal is a byte-exact provenance copy. Full frozen
verification intentionally requires the original preregistered absolute path
`/mnt/hswm/evidence/hswm-g1-micro-exploratory-2026-08-30-consumption-v1`;
substituting the repository copy is rejected. The relative result bundle and
its two SQLite stores remain locally structurally replayable.

Running the verifier by file path initially exposed a CLI import bootstrap
defect. The exact live-source verifier was successfully run with
`PYTHONPATH=.`; a post-occurrence patch only makes the documented path-form CLI
establish the repository root itself. That repair does not change the frozen
protocol, execution, bundle, or scientific interpretation.

## Scientific boundary and next experiment

The formal status remains:

- claim ceiling: `INSTRUMENT_VALIDATION_ONLY`;
- G0: `NOT_PASSED`;
- G1: `NOT_EVALUATED`;
- causal learning efficacy: not established;
- complete G1 control family and powered analysis: not run.

The smallest useful next study changes the task contract, not the harness. A
fresh preregistration should replace value-revealing answer labels with
episode-secret opaque action codes whose mapping is held only by the evaluator.
For the identifiability pilot, the collapsed sham arm should be replaced by an
explicitly named `FORCED_OPPOSITE_FEEDBACK` control; it must not be described as
outcome-independent sham. The same five-branch mechanics can then be run across
a small exploratory set of independent episodes to estimate single-item
baseline saturation and treatment-control separation before any powered G1
confirmatory run. A later complete G1 design still needs its separately defined
outcome-independent sham control.

This occurrence does not test or change the eight FCL laws,
HSWM-of-HSWMs same-rule recursive composition, consciousness, selfhood, or
scale-invariant causal closure. The fractal research status remains
`SCIENTIFICALLY_CONNECTED / INTEGRATED_CLAIM_UNJUDGED`, and the live KG is not
mutated.
