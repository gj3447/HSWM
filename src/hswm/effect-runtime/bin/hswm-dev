#!/bin/sh
# Checkout launcher: runtime work is performed by the native Effect process.
hswm_script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PATH="$hswm_script_dir:$PATH" exec node "$hswm_script_dir/../dist/hswm-dev-process.js" "$@"
