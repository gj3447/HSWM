# Verification

This directory is the canonical home for cross-cutting verification assets that
are not ordinary unit tests or formal proofs. The current executable checks live
in [`tests/`](../tests/) and [`formal/`](../formal/); new verification artifacts
belong here instead of the repository root.
